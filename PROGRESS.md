# Badaguan Three.js Implementation Progress

This file is the executable checklist and status authority for the implementation described by `PLAN.md`. It mirrors exactly 12 sequential chunks and 36 canonical implementation tasks. Canonical task wording is copied verbatim; do not reorder, rename, paraphrase, merge, split, or silently omit it.

## Status vocabulary

Every chunk and review record MUST use exactly one status: `not started`, `in progress`, `blocked`, or `complete`.

A chunk is `complete` only when all three canonical tasks are checked with implementation evidence; every clause of that chunk’s PLAN `Acceptance` is proven in the acceptance evidence; every PLAN `Focused verification` command and scenario has a concrete result in the focused-verification evidence; the declared review surface has received an implementation review; every required implementation and fix commit is recorded; and the latest append-only review round is a dated/reviewer-attributed `clean` result with no new findings. A finding-bearing review result can never close a chunk. Partial work remains unchecked.

## Mandatory resume and sequence protocol

1. Read `PLAN.md` and this file before implementation.
2. Work only on the earliest incomplete chunk. C02–C12 each depend on the immediately preceding chunk being `complete`; no other dependency-ready interpretation exists.
3. Set that chunk to `in progress`, implement and prove its canonical tasks, complete all acceptance and focused-verification evidence, run the declared implementation review, fix and commit every finding, and append re-review rounds until the latest result is `clean`.
4. Record exact commands/manual scenarios, environment/viewport/tier, observed result, artifact path or URL where relevant, and session/date. Compilation or inspection alone is not behavioral or visual proof.
5. Record the required implementation commit(s) and each review-fix commit independently. Set the chunk `complete` only after every completion predicate above is true; only then may the next chunk begin.
6. If the earliest incomplete chunk is blocked, set it to `blocked` and record the blocker, evidence, owner/missing prerequisite, and exact unblock condition. All later chunks remain blocked by sequence; there is no blocked-skip behavior.

## Evidence and review-round convention

- Each task evidence slot records implementation proof for that exact task. Use `pending` until proof exists.
- Acceptance evidence MUST address every semicolon-separated or otherwise distinct PLAN acceptance clause, with no clause inferred from another field.
- Focused-verification evidence MUST record every listed command and manual/browser/visual scenario, its environment, and concrete result.
- Review rounds are append-only. Never overwrite or delete an earlier round. Add rows as needed; each finding-bearing round is followed by a fix/re-review round that records finding IDs, fix evidence, the fix commit, reviewer/date, and either `clean` or the complete new finding IDs.
- `not applicable` is not a tracker-only decision. It requires a committed amendment to `PLAN.md` that removes or changes the obligation, and the amendment commit must be cited here.

---

## C01 — Establish the browser application foundation

- **Status:** `complete`
- **Session/date:** 2026-07-12
- **Dependencies:** none
- [x] Scaffold the Vite + TypeScript + Three.js application, lock dependency versions, and add the repository/toolchain configuration.
  - **Implementation evidence:** `package.json:1-41` defines the private native-ES-module Vite application, exact `three@0.185.1` runtime dependency, exact TypeScript/Vite/Vitest/Playwright toolchain pins, supported Node `>=24 <25` range, exact `npm@11.16.0` package manager, and fail-closed `devEngines` enforcement for Node `24.18.0` and npm `11.16.0`; `.nvmrc:1` pins Node `24.18.0`; `package-lock.json:1-22` records the synchronized root metadata and the lockfile records resolved/integrity data; `.gitignore:1-39`, `tsconfig.json:1-29`, `vite.config.ts:1-10`, `vitest.config.ts:1-10`, `playwright.config.ts:1-54`, and `ATTRIBUTION.md:3-37` provide generated-file exclusions, strict/build/test configuration, fail-closed unit discovery, owned browser-server configuration, the procedural baseline, nine research citations, and dependency/license/asset provenance.
- [x] Create the semantic HTML shell, global responsive CSS, application entry point, and typed module boundaries defined by PLAN.md.
  - **Implementation evidence:** `index.html:1-80` supplies the semantic document shell, processed stylesheet link, local-only CSP meta policy, metadata, header/About disclosure, main experience, labeled canvas, one status region, controls region, footer, and authoritative noscript content; `src/ui/styles.css:1-309` supplies responsive sizing/layout, visible focus styling with an inset future-canvas ring, direct no-script fallback styling, and status suppression when the parsed noscript block is present; `src/main.ts:1-41` defines the typed shell boundary and bootstrap guards without a JavaScript-only stylesheet import; `src/ui/shellContract.ts:1-64` defines ready/disabled presentations, preserves `#app-status` as the sole polite atomic channel, removes controls live/busy attributes, and removes stale canvas tabindex.
- [x] Add focused unit, browser, and static-check commands plus the baseline unsupported-JavaScript/canvas description.
  - **Implementation evidence:** `package.json:22-28` exposes `test:unit`, `test:browser`, `test:visual`, `typecheck`, `build`, and `dev`; `vitest.config.ts:1-10` defines the Node unit-test environment and fail-closed `tests/unit/**/*.test.ts`/`tests/unit/**/*.spec.ts` globs; `playwright.config.ts:1-54` defines the local web server, strict port ownership, browser projects, and artifact behavior; `tests/unit/shellContract.test.ts:1-72` provides typed ready/disabled shell-contract coverage; and `index.html:36-44,66-76` provides the meaningful canvas alternative and visible JavaScript-disabled Badaguan description.
- **PLAN acceptance contract:** clean install is reproducible; strict type checking and production build work; page displays meaningful HTML before/without a WebGL scene; only declared dependencies exist; no framework or remote runtime asset is introduced.
  - **Acceptance evidence (every clause):** (1) Clean install is reproducible: parent-observed `npm ci` passed under exact Node `24.18.0` and npm `11.16.0`; `.nvmrc:1`, `package.json:6-20` fail-closed `devEngines`, and `package-lock.json:1-22` provide exact runtime/package-manager/root dependency metadata and the lockfile records resolved/integrity data, with the dependency/license/provenance inventory in `ATTRIBUTION.md:7-37`. (2) Strict type checking and production build work: parent-observed `npm run typecheck` passed (`artifact://108`), parent-observed `npm run build` passed (`artifact://109`), and strict/no-emit plus Vite output settings are in `tsconfig.json:2-21` and `vite.config.ts:3-9`. (3) The page displays meaningful HTML before/without a WebGL scene: `index.html:18-76` contains the Badaguan shell, explanatory copy, status, controls, footer, canvas alternative, and noscript description before module execution; parent browser inspection at both required viewports confirmed the enabled ready shell and the disabled styled fallback, including the meaningful canvas alternative remaining in the DOM. (4) Only declared direct dependencies exist: `package.json:30-39` declares only `three` at runtime and the complete exact-pinned dev-tool list, while `package-lock.json` records the reproducible transitive graph and the parent-observed install passed. (5) No framework or remote runtime asset is introduced: the implementation uses native TypeScript/DOM plus Three.js, `index.html:11-14,78` contains a local-only CSP and local module/style references, and `ATTRIBUTION.md:3-23` records no remote runtime assets while documenting nine research URLs as provenance-only citations; parent inspection confirmed the CSP meta is local-only with loopback Vite websocket allowances.
- **PLAN focused verification contract:** `npm ci`; `npm run typecheck`; `npm run build`; `npm run dev -- --host 127.0.0.1` then inspect at 320×568 and 1280×720 with JavaScript disabled and enabled.
  - **Focused-verification evidence (every command/scenario):** (1) `npm ci` — parent-observed pass under Node `24.18.0`/npm `11.16.0`. (2) `npm run typecheck` — parent-observed pass; output captured at `artifact://108`. (3) `npm run build` — parent-observed pass; output captured at `artifact://109`. (4) `npm run dev -- --host 127.0.0.1` — parent started the local loopback Vite server and completed the required browser inspection at `320×568` and `1280×720`. (5) JavaScript enabled at `320×568` — linked CSS applied with computed `html` background `rgb(16, 47, 50)`, one canvas, visible meaningful ready status, exactly one live region, controls without `aria-live` or `aria-busy`, canvas without `tabindex`, and no horizontal or vertical overflow. (6) JavaScript enabled at `1280×720` — the same linked-CSS, one-canvas, ready-status, sole-live-region, controls-cleanup, canvas-focusability, and no-overflow results; CSP meta inspection also confirmed the local-only policy with loopback development websocket allowances. (7) JavaScript disabled at `320×568` — linked CSS still applied with computed `html` background `rgb(16, 47, 50)`, `#app-status` display was `none`, `.no-script` was visible with the authoritative disabled explanation, the meaningful canvas alternative remained in the DOM, and there was no horizontal or vertical overflow. (8) JavaScript disabled at `1280×720` — the same styled fallback passed: status display `none`, visible authoritative `.no-script` explanation, meaningful canvas alternative in the DOM, and no horizontal or vertical overflow.
- **PLAN review surface:** dependency necessity/licenses, generated output exclusions, semantic shell, focus styling, initial text, module boundaries. Do not review scene quality yet.
  - **Implementation-review evidence:** Round 1 packets: `agent://C01DependencyReviewer`, `agent://C01ConfigReviewer`, `agent://C01ShellReviewer`, and `agent://C01AccountingReviewer`; findings DEP-001, CFG-001, CFG-002, UX-001–UX-004, and ACC-001–ACC-003 were reviewed, and the implemented fixes plus parent verification are mapped in the round-1 fix evidence below. The parent-observed toolchain/unit/browser checks passed. Round 2 read-only re-review of reviewed fix commit `72f1a12`, with accounting linkage `066f54e`, is clean: `agent://C01DependencyReReviewer`, `agent://C01ConfigReReviewer`, `agent://C01ShellReReviewer`, and `agent://C01AccountingReReviewer` each report `clean` with no new findings; DEP-001 and ACC-002 are resolved on the dependency/provenance surface, CFG-001 and CFG-002 on the test/configuration surface, UX-001–UX-004 on the shell/focus/live-region/CSP surface, and ACC-001–ACC-003 on the accounting surface.
- **Planned commit boundary:** `chore: scaffold the Badaguan Three.js application`
- **Implementation commit hash(es):** `c52fdbc` (draft; implementation commit), `72f1a12` (review-fix commit).
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | DEP-001, CFG-001, CFG-002, UX-001, UX-002, UX-003, UX-004, ACC-001, ACC-002, ACC-003 | Round 1 review packets: `agent://C01DependencyReviewer`, `agent://C01ConfigReviewer`, `agent://C01ShellReviewer`, and `agent://C01AccountingReviewer`; fix verification: DEP-001 — `.nvmrc:1` is pinned to Node `24.18.0`, `package.json:6-20` adds exact fail-closed Node/npm `devEngines`, and `package-lock.json:1-22` has synchronized root metadata; parent `npm ci` passed under Node `24.18.0`/npm `11.16.0`. CFG-001 — `test:unit` is `vitest run`, `vitest.config.ts:4-9` no longer enables `passWithNoTests`, and the real `tests/unit/shellContract.test.ts` is covered; parent unit verification passed one file/two tests. CFG-002 — `playwright.config.ts:28-32` sets `reuseExistingServer: false` while retaining strict-port local ownership; parent local-dev browser smoke completed at both required viewports. UX-001 — `index.html:14` loads CSS independently of JavaScript, `src/main.ts:1` has no stylesheet side-effect import, the loading copy is `Loading Badaguan…`, and `src/ui/styles.css:215-230` suppresses the boot status when the parsed noscript explanation is present; parent disabled-JavaScript reruns passed at `320×568` and `1280×720` with linked CSS, status hidden, visible authoritative `.no-script`, no overflow, and the canvas alternative in the DOM. UX-002 — `index.html`/`src/ui/shellContract.ts:53-55` leave the canvas without tabindex and remove stale tabindex, while `src/ui/styles.css:158-161` provides an inset future-canvas focus ring; parent enabled-JavaScript inspection confirmed no canvas tabindex at both viewports. UX-003 — `index.html:53-58` and `src/ui/shellContract.ts:56-63` reserve the sole polite atomic channel for `#app-status` and remove controls `aria-live`/`aria-busy`; parent enabled-JavaScript inspection found exactly one live region and clean controls at both viewports. UX-004 — `index.html:11-14` adds a local-only CSP with same-origin resource limits, loopback Vite websocket allowances, `object-src 'none'`, and `base-uri 'none'`; parent inspection confirmed the CSP meta and its local-only/loopback policy. ACC-001 — the current closure roll-up is synchronized to `Overall status: in progress` and `Canonical implementation tasks checked: 3/36`, while `Chunks complete` remains `0/12`. ACC-002 — `ATTRIBUTION.md:7-23` now contains the procedural visual/asset baseline and PLAN §16 research citations 1–9, explicitly stating the URLs are provenance-only and not runtime fetches; read-back confirmed the citations and dependency ledger. ACC-003 — the previously invalidated JavaScript-disabled `320×568` and `1280×720` focused-verification entries were rerun after the CSS/noscript fix and both passed with the styled fallback results recorded above. | `72f1a12` | C01DependencyReviewer; C01ConfigReviewer; C01ShellReviewer; C01AccountingReviewer / 2026-07-12 | DEP-001, CFG-001, CFG-002, UX-001, UX-002, UX-003, UX-004, ACC-001, ACC-002, ACC-003 |
| 2 | DEP-001, CFG-001, CFG-002, UX-001, UX-002, UX-003, UX-004, ACC-001, ACC-002, ACC-003 | Clean read-only re-review of reviewed fix commit `72f1a12` with accounting linkage `066f54e`; `agent://C01DependencyReReviewer`, `agent://C01ConfigReReviewer`, `agent://C01ShellReReviewer`, and `agent://C01AccountingReReviewer` all report `clean` with no new findings. Parent verification remains green at `artifact://108` and `artifact://109`, plus the one-file/two-test unit run and required browser/focus scenarios. | `72f1a12` (reviewed fix; `066f54e` accounting linkage) | C01DependencyReReviewer, C01ConfigReReviewer, C01ShellReReviewer, C01AccountingReReviewer / 2026-07-12 | clean |

- **Latest review round is clean:** yes
- **Blocker:** none
- **Unblock condition:** none
---

## C02 — Implement lifecycle, capability, and UI state foundations

- **Status:** `complete`
- **Session/date:** 2026-07-12
- **Dependencies:** C01 complete
- [x] Implement the explicit application state machine and event-driven transitions for boot, loading, onboarding, exploring, paused, degraded, context-lost, unsupported, and fatal states.
  - **Implementation evidence:** `src/app/appState.ts:1-387` defines the typed nine-state `AppState` union, complete `AppEvent` set, visible-output/action invariants, safe `accepted`/`transitioned` results, terminal precedence, degraded underlying projections, panel transitions, and deterministic retry; `src/app/AppController.ts:1-174` owns event-driven BOOT, capability, Retry, forced-scenario, Escape, and UI-action dispatch; `tests/unit/appState.test.ts:1-331` exercises every state/event matrix plus nonblank projections, readiness/start gating, lock/fallback, pause/resume, precedence, degraded mode, retry, illegal/duplicate input, exact copy, and panels. Parent `npm run test:unit -- appState` passed 1 file/10 tests; parent `npm run typecheck` passed at `artifact://162` and final `npm run build` passed at `artifact://165`.
- **Round-1 fix verification (STATE-001, TEST-003):** Parent reran `npm run test:unit -- appState` with 1 file/10 tests passing. The exhaustive transition oracle in `tests/unit/appState.test.ts:99-125` iterates every enriched state in `TRANSITION_STATES` against every event in `ALL_EVENTS`, asserting exact next state, `accepted`, `transitioned`, unchanged object identity for rejected events, and invariant equality; the remaining unit cases in `tests/unit/appState.test.ts:127-331` cover locked Escape pause/resume, unlock-during-pause, fatal/context-loss precedence, degraded projections, deterministic Retry, illegal/duplicate events, exact copy, and panels. Parent `npm run typecheck` passed at `artifact://202`; parent `npm run build` passed at `artifact://203`.
- [x] Implement the DOM overlay system, live status region, focus management, onboarding/help/settings panels, and deterministic Start/Retry/Resume actions; onboarding identifies Badaguan and visibly explains WASD/arrows, mouse or drag/touch look, and Escape, while locked, unlocked, and pointer-lock-fallback modes have concise visible status.
  - **Implementation evidence:** `index.html:18-97` supplies the semantic overlay shell, actions/panels/controls regions, canvas description, and sole `#app-status`; `src/app/config.ts:1-23` owns the exact onboarding, locked, fallback, paused, retry, and failure copy; `src/ui/AppUI.ts:1-980` implements state projections, Start/Pause/Resume/Retry actions, Help/Settings fragments, keyboard Escape handling, one polite atomic status channel, transition-key announcement deduplication, ready-only Start focus, panel-heading focus, and trigger focus return; `src/app/AppController.ts:55-103` routes Escape and UI actions through the reducer. `src/ui/AppUI.ts:187-205,309-447,451-677,680-975` specifically covers fallback wording, every projection/action, panel content, and rendering/focus/status behavior.
- **Round-1 fix verification (UI-001..003, TEST-002, TEST-004):** The corrected lifecycle browser rerun passed 32/32 across desktop Chromium 1280×720 and mobile Chromium 390×844. The exact keyboard-only flow passed with both Enter and Space: ready onboarding Start focus → activation → exploring → open Help → Tab reaches only Close → Shift+Tab leaves overlay/controls → Escape closes Help and returns focus to the Help trigger → Escape pauses → Resume activation restores exploring; the unsupported→supported Retry path then reaches onboarding. Help and Settings set overlay and controls inert; background actions are unreachable, only Close is keyboard-reachable before body content, and Escape returns focus to the opening trigger. Degraded Start remains focused and Enter/Space activates it; locked Escape enters paused state with the locked-pause copy and visible Resume.
- [x] Gate startup on WebGL2 capability and render a useful non-WebGL fallback without constructing the Three.js runtime.
  - **Implementation evidence:** `src/platform/capabilities.ts:1-62` provides the typed WebGL2 probe/result seam, fail-closed exception handling, and best-effort context cleanup; `src/app/AppController.ts:35-44,105-117` gates capability evaluation and exposes `lifecycle=unsupported` only in DEV; `src/main.ts:3-25` exposes the typed capability overrides only in DEV; `tests/browser/lifecycle.spec.ts:215-223` verifies `/?capability=unsupported-then-supported`: the full-union marker selector `[data-three-runtime], canvas[data-renderer], [data-renderer-runtime], [data-runtime-created]` has count 0 before `Retry` in exact `unsupported` state and count 0 again after `Retry` reaches supported onboarding with focused `Start`; the latest `npm run test:browser -- lifecycle.spec.ts` rerun passed 32/32 in 7.0s across desktop Chromium 1280×720 and mobile Chromium 390×844; final production `dist` inspection after `artifact://165` found no `capability=supported`, `lifecycle=`, `data-three-runtime`, or `data-renderer` seam/marker strings.
- **Round-1 fix verification (TEST-001, ACC-004):** The typed DEV-only `unsupported-then-supported` seam starts in unsupported, Retry re-runs capability evaluation, and reaches supported onboarding. `tests/browser/lifecycle.spec.ts:215-223` asserts the full-union marker selector `[data-three-runtime], canvas[data-renderer], [data-renderer-runtime], [data-runtime-created]` has count 0 before `Retry` in exact `unsupported` state and count 0 again after `Retry` reaches supported onboarding with focused `Start`; the latest lifecycle rerun passed 32/32 in 7.0s across desktop Chromium 1280×720 and mobile Chromium 390×844. Final production `dist` inspection after `artifact://203` found no DEV seam query strings (`capability=supported`, `capability=unsupported-then-supported`, or `lifecycle=`) and no runtime marker strings (`data-three-runtime`, `data-renderer`, or `data-runtime-created`).
- **PLAN acceptance contract:** every state has a visible, non-blank projection; onboarding identifies Badaguan and visibly explains mouse look, drag/touch fallback, WASD/arrows, and Escape; confirmed lock shows “Press Escape to release”; unlocked and denied/error states visibly name drag + keyboard or touch exploration; Start works by click, Enter, and Space and is focused only when ready; illegal transitions fail safely; retry is deterministic; a forced WebGL2-negative path never constructs the renderer; status announcements are transition-based.
  - **Acceptance evidence (every clause):** (1) Every state has a visible nonblank projection: `src/app/appState.ts:83-173` maps boot, loading, onboarding, exploring, paused, degraded, context-lost, unsupported, and fatal to nonblank invariant output, `src/ui/AppUI.ts:309-417` renders each projection, and `tests/unit/appState.test.ts:109-125` checks every state and event result for trimmed nonblank output; the parent unit run passed 1 file/10 tests. (2) Onboarding identifies Badaguan and visibly explains mouse look, drag/touch fallback, WASD/arrows, and Escape: `src/app/config.ts:4-8` and `src/ui/AppUI.ts:95-109,335-344` provide the exact detail and guide, and parent manual browser inspection showed the exact onboarding copy at 1280×720 and 390×844. (3) Confirmed lock shows exactly `Press Escape to release`: `src/app/config.ts:6,19-22` and `src/ui/AppUI.ts:347-360` select that detail/status, and the forced locked browser projection plus manual inspection matched it. (4) Unlocked and denied/error projections visibly name drag plus keyboard or touch: `src/ui/AppUI.ts:187-195` supplies distinct denied/error/unlocked sentences, and parent manual inspection plus the forced denied/error/unlocked browser projections showed those fallback details/statuses. (5) Start works by click, Enter, and Space and is focused only when ready: `src/ui/AppUI.ts:427-447,701-724,905-936` emits Start only when `canStart` and focuses it on the first ready render; `tests/browser/lifecycle.spec.ts:27-43,62-119` covers ready focus, the pointer-click Start flow, Enter, Space, and Resume, while parent manual inspection confirmed Start focus only in ready onboarding. (6) Illegal transitions fail safely: `src/app/appState.ts:176-184,187-386` returns the unchanged state with `accepted:false`/`transitioned:false` for rejected events, `tests/unit/appState.test.ts:276-290` proves illegal and duplicate input safety, and `tests/browser/lifecycle.spec.ts:140-153` proves panel-only/illegal Escape leaves onboarding unchanged with zero status mutations. (7) Retry is deterministic: `src/app/appState.ts:311-385` sends retryable context-lost/degraded/unsupported/fatal states to loading and `src/app/AppController.ts:90-117` reevaluates capabilities after accepted Retry; `tests/unit/appState.test.ts:260-274` and `tests/browser/lifecycle.spec.ts:215-223` prove the deterministic before/after-Retry result. (8) A forced WebGL2-negative path never constructs the Three.js runtime: `src/app/AppController.ts:105-117` dispatches unsupported from the capability result before any runtime integration, `src/main.ts:3-25` keeps the capability overrides DEV-only, and `tests/browser/lifecycle.spec.ts:215-223` asserts `/?capability=unsupported-then-supported` with the full-union marker selector `[data-three-runtime], canvas[data-renderer], [data-renderer-runtime], [data-runtime-created]` at count 0 before `Retry` in exact `unsupported` state and count 0 again after `Retry` reaches supported onboarding with focused `Start`; the latest lifecycle rerun passed 32/32 in 7.0s across desktop Chromium 1280×720 and mobile Chromium 390×844. Production `dist` grep found no `capability=supported`, `lifecycle=`, `data-three-runtime`, or `data-renderer` strings. (9) Status announcements are transition-based: `src/ui/AppUI.ts:218-235,701-724,895-936` keys announcements by state/projection and changes the sole atomic polite status only for a new key; parent manual inspection and browser assertions found exactly one `[aria-live]`/`#app-status` region at both required viewports. (10) Separate manual browser checks, outside the automated 24/24 suite, covered the previously structural-only projections: after Start, degraded showed nonblank `REDUCED MODE · EXPLORING`, detail naming drag plus keyboard/touch exploration, Pause/Retry/Help/Settings actions, and a persistent reduced-mode status; screenshot `local://c02-evidence/c02-degraded-1280x720.png`. Context-lost showed nonblank graphics-recovery detail/status with Retry and Help; screenshot `local://c02-evidence/c02-context-lost-1280x720.png`. Fatal showed nonblank stopped-state detail/status with the forced reason, Retry and Help; screenshot `local://c02-evidence/c02-fatal-1280x720.png`. These are manual observations, not automated browser assertions.
- **Round-1 acceptance fix verification (STATE-001, UI-001..003, TEST-001..004, ACC-001..004; prior acceptance evidence retained):** Parent verification confirms every state projection remains nonblank and every rejected transition remains safe under the exhaustive oracle. The final manual matrix at 1280×720 covered onboarding, locked, denied, unsupported, degraded, context-lost, and fatal; onboarding was also checked at 390×844. Each projection had exactly one live region, no horizontal/vertical overflow, and no settled `aria-busy`; locked showed exactly `Press Escape to release`; denied/unlocked/error visibly offered drag plus keyboard or touch; degraded kept Start focused and usable by Enter and Space; context-lost exposed nonblank status/detail with `restart is required`, Retry, and Help while not busy; fatal exposed nonblank status/detail, Retry, and Help while not busy. Announcement mutation proof is exact: panel-only open/close and illegal Escape produced 0 status mutations, while one onboarding→exploring lifecycle transition produced exactly 1. The drawer checks found overlay and controls inert, only Close keyboard-reachable before body content, background actions unreachable, and Escape returning focus to the trigger. At 320×568 with JavaScript disabled, the overlay and status were hidden, `#app-status` was empty, no busy state settled, and the authoritative `<noscript>` fallback was visible in `local://c02-evidence/c02-nojs-320x568.png`. Final production seam absence and the refreshed durable screenshot set are recorded below.
- **Round-2 acceptance fix verification (TEST-005, UI-004, UI-005, ACC-005, ACC-006; prior acceptance evidence retained):** Settings automation in `tests/browser/lifecycle.spec.ts:155-179` opens Settings with Space, asserts the panel is visible and `aria-expanded="true"`, asserts both `#app-overlay` and `#app-controls` have `inert`, presses Tab and verifies focus is on Close, presses Shift+Tab and verifies the active element is outside both inert regions, then presses Escape and verifies the panel is hidden, focus returns to the Settings trigger, `aria-expanded="false"`, and both inert attributes are removed. The post-assertion lifecycle rerun passed 32/32 in 6.9s. With JavaScript disabled at 320×568, DOM checks found the canvas, `#app-overlay`, and `#app-status` `display:none`; one authoritative no-JS narrative remained visible with no horizontal/vertical overflow, captured at `local://c02-evidence/c02-nojs-320x568.png`. At 1280×720, DOM measurements for both unsupported and fatal notices placed each complete notice fully inside its panel; the panel had `overflowY:auto`, `clientHeight=482`, and `scrollHeight=493` as the vertical overflow affordance, while the page had no horizontal/vertical overflow. Refreshed captures are `local://c02-evidence/c02-unsupported-1280x720.png` and `local://c02-evidence/c02-fatal-1280x720.png`. Parent static checks passed with typecheck `artifact://268` and build `artifact://269`. These findings are now concretely evidenced; Round-2 fix evidence and commit `2dca825` are recorded, while an independent terminal-clean re-review remains pending. The new pointer-click Start scenario is at `tests/browser/lifecycle.spec.ts:62-76`; final click/range verification remains pending in Round 3.
- **PLAN focused verification contract:** `npm run test:unit -- appState`; `npm run test:browser -- lifecycle.spec.ts`; verify the exact visible onboarding/control/locked/unlocked/denied-fallback copy; keyboard-only scenario through Start by Enter and Space, Help, Escape, Retry; forced unsupported capability scenario.
  - **Focused-verification evidence (every command/scenario):** (1) `npm run test:unit -- appState` — parent-observed pass, 1 file/10 tests. (2) `npm run test:browser -- lifecycle.spec.ts` — the initial exact command was blocked at `artifact://154` because the strict Vite server could not bind `127.0.0.1:4173` (`Port 4173 is already in use`) from another user-owned project; the fix in `playwright.config.ts:3-25,48-52` derives one validated port from `PLAYWRIGHT_PORT` (integer 1–65535) or default `41731`, uses it consistently for Playwright `baseURL`, `webServer.url`, and Vite `--port`, and retains loopback, `--strictPort`, and `reuseExistingServer:false`; rerunning the same command on validated `127.0.0.1:41731` passed 24/24 in 5.5s across desktop Chromium 1280×720 and mobile Chromium 390×844. (3) Exact visible copy/manual checks — at 1280×720 and 390×844 onboarding showed the exact Badaguan/WASD/arrow/mouse/drag-touch/Escape copy, Start was focused only when ready, exactly one live region existed, the canvas had no `tabindex`, and there was no horizontal or vertical overflow; locked detail/status was exactly `Press Escape to release`; denied detail/status visibly offered drag plus keyboard or touch; unsupported showed the actionable WebGL2 message and Retry. (4) Keyboard-only/browser lifecycle coverage — the recorded passing suite covers Start by Enter and Space, Help open/close without an optimistic lifecycle change, Settings close returning focus to its trigger, Escape pause/resume, click Start, Retry, safe illegal input, forced locked/denied/error/unlocked/unsupported projections, sole-live-region/canvas-focusability assertions, and unsupported before/after Retry; the explicit pointer-click Start scenario is recorded at `tests/browser/lifecycle.spec.ts:62-76`, with final click/range verification pending Round 3; reducer/invariant unit coverage and `AppUI` projection code cover every state structurally, while no browser scenario is claimed here for forced degraded/context-lost/fatal projections. (5) Forced unsupported capability — `tests/browser/lifecycle.spec.ts:215-223` drives `/?capability=unsupported-then-supported` and asserts the full-union marker selector `[data-three-runtime], canvas[data-renderer], [data-renderer-runtime], [data-runtime-created]` has count 0 before `Retry` in exact `unsupported` state and count 0 again after `Retry` reaches supported onboarding with focused `Start`; the latest lifecycle rerun passed 32/32 in 7.0s across desktop Chromium 1280×720 and mobile Chromium 390×844. The production `dist` grep after `artifact://165` found no `capability=supported`, `lifecycle=`, `data-three-runtime`, or `data-renderer` strings. (6) Parent static verification — `npm run typecheck` passed at `artifact://162`; final `npm run build` passed at `artifact://165`. (7) Separate manual browser projections, outside the automated 24/24 suite — after Start, degraded displayed nonblank `REDUCED MODE · EXPLORING`, drag plus keyboard/touch detail, Pause/Retry/Help/Settings, and a persistent reduced-mode status; screenshot `local://c02-evidence/c02-degraded-1280x720.png`. Context-lost displayed nonblank graphics-recovery detail/status with Retry and Help; screenshot `local://c02-evidence/c02-context-lost-1280x720.png`. Fatal displayed nonblank stopped-state detail/status with the forced reason, Retry and Help; screenshot `local://c02-evidence/c02-fatal-1280x720.png`. These observations are manual proof and do not expand the automated browser claim beyond locked/denied/error/unlocked/unsupported scenarios.
- **Round-1 focused-verification fix record (prior command/scenario evidence retained):** `npm run test:unit -- appState` passed 1 file/10 tests with the exhaustive state/event oracle. `npm run test:browser -- lifecycle.spec.ts` passed 32/32 in 7.0s after the corrected exact locked-paused assertion, across desktop Chromium 1280×720 and mobile Chromium 390×844. The keyboard-only sequence was exercised with both Enter and Space exactly as recorded above, including Help/Settings drawer inertness and tab order, trigger focus return, Escape pause/resume, degraded Start focus, and unsupported→supported Retry. The current marker proof in `tests/browser/lifecycle.spec.ts:215-223` uses the full-union selector `[data-three-runtime], canvas[data-renderer], [data-renderer-runtime], [data-runtime-created]` with count 0 before `Retry` in exact `unsupported` state and count 0 after `Retry` reaches supported onboarding with focused `Start`. MutationObserver counts were 0 for panel-only and illegal Escape changes and 1 for the lifecycle transition. Forced onboarding/locked/denied/unsupported/degraded/context-lost/fatal checks confirmed the required copy, one live region, no overflow, no settled busy, restart-required context-loss wording, and actionable Retry/Help. The no-JS 320×568 run confirmed hidden overlay/status, empty status, no busy, and the durable fallback screenshot; production `dist` inspection confirmed no DEV seam/runtime markers. The explicit pointer-click Start scenario is at `tests/browser/lifecycle.spec.ts:62-76`; click/range verification remains pending Round 3.
- **Round-2 focused-verification fix record (TEST-005, UI-004, UI-005, ACC-005, ACC-006; prior command/scenario evidence retained):** (1) `npm run test:browser -- lifecycle.spec.ts` — parent rerun passed 32/32 in 6.9s after the Settings assertions; the Settings scenario verified visible/expanded Settings, `inert` on both `#app-overlay` and `#app-controls`, Tab focus on Close, Shift+Tab focus outside both inert regions, Escape close, trigger focus return, collapsed `aria-expanded`, and removal of both inert attributes. (2) JavaScript-disabled 320×568 browser check — canvas/overlay/status were `display:none`, one authoritative no-JS narrative was visible, and no horizontal/vertical overflow was observed; screenshot `local://c02-evidence/c02-nojs-320x568.png`. (3) Unsupported and fatal 1280×720 browser checks — each refreshed screenshot showed a complete notice; DOM measurements placed each notice fully inside its panel, with panel `overflowY:auto`, `clientHeight=482`, `scrollHeight=493`, and no page overflow; screenshots `local://c02-evidence/c02-unsupported-1280x720.png` and `local://c02-evidence/c02-fatal-1280x720.png`. (4) `npm run typecheck` — pass at `artifact://268`; `npm run build` — pass at `artifact://269`. This is fix evidence only; Round-2 fix evidence and commit `2dca825` are recorded, while an independent terminal-clean re-review remains pending. The explicit pointer-click Start flow is now cited at `tests/browser/lifecycle.spec.ts:62-76`; final click/range verification remains pending in Round 3.
- **Durable visual evidence (refreshed and retained):** `local://c02-evidence/c02-onboarding-1280x720.png` (onboarding desktop); `local://c02-evidence/c02-onboarding-390x844.png` (onboarding mobile); `local://c02-evidence/c02-locked-1280x720.png` (locked); `local://c02-evidence/c02-denied-1280x720.png` (denied); `local://c02-evidence/c02-unsupported-1280x720.png` (unsupported); `local://c02-evidence/c02-degraded-1280x720.png` (degraded); `local://c02-evidence/c02-context-lost-1280x720.png` (context-lost); `local://c02-evidence/c02-fatal-1280x720.png` (fatal); `local://c02-evidence/c02-nojs-320x568.png` (JavaScript-disabled fallback). All C02 captures in `local://c02-evidence/` were refreshed for the final manual checks; screenshots preserve the prior visual history and are not treated as a clean re-review or commit claim.
- **Acceptance re-verification status (Round 1):** Parent fix verification resolved every pending acceptance hold for STATE-001, UI-001..003, TEST-001..004, and ACC-001..004. The verified record covers the exhaustive transition oracle; deterministic DEV-only unsupported→supported Retry; exact announcement mutation counts; locked Escape pause; drawer inert/tab-order/focus return; degraded Start focus; context-lost restart-required and fatal actionable copy; one-live-region/no-overflow/no-settled-busy checks; production seam absence; the final no-JS fallback; and refreshed durable screenshots. Original acceptance evidence remains retained above; independent implementation re-review is still pending.
- **Focused-verification re-verification status (Round 1):** Parent verification resolved every pending focused hold: the appState unit pass (1 file/10 tests with exhaustive transition oracle), lifecycle browser rerun (32/32 desktop/mobile after the corrected locked-paused assertion), exact Enter/Space keyboard-only Help/Escape/Retry flow, 0/0/1 announcement mutation counts, locked Escape pause, drawer inert/tab-order/focus checks, degraded/context-lost/fatal checks, unsupported→supported Retry re-evaluation, production seam inspection, and final no-JS 320×568 screenshot. Original focused evidence remains retained above; independent implementation re-review is still pending.
- **PLAN review surface:** state/event completeness, focus ownership, DOM semantics, fallback wording, absence of hidden optimistic transitions.
- **Implementation-review evidence:** Round 1 packets for draft commit `1a0dcfb`: `agent://C02StateReviewer` (STATE-001), `agent://C02UIReviewer` (UI-001..003), `agent://C02TestReviewer` (TEST-001..004), and `agent://C02AccountingReviewer` (ACC-001..004); all prior Round-1 fix verification and evidence remain retained above. Round 2 re-review packets: `agent://C02StateReReviewer` is clean with no new state findings, while `agent://C02UIReReviewer` reports UI-004/UI-005, `agent://C02TestReReviewer` reports TEST-005, and `agent://C02AccountingReReviewer` reports ACC-005/ACC-006. Round-2 acceptance/focused fix evidence is now recorded above, including Settings isolation assertions, the single authoritative no-JS presentation, complete unsupported/fatal notices, refreshed captures, and artifacts `artifact://268`/`artifact://269`. Round-2 fix evidence and commit `2dca825` are recorded; the combined result remains finding-bearing pending independent terminal-clean re-review. Round 3 terminal review packets `agent://C02TerminalStateReview`, `agent://C02TerminalUIReview`, `agent://C02TerminalTestReview`, and `agent://C02TerminalAccountingReview` report findings TEST-006 (pointer-click Start coverage/verification) and ACC-007 (stale or shifted C02 source/test ranges); Round-3 fix evidence and review-fix commit `f863c37` are recorded, with terminal-clean re-review pending. No clean or completion claim is made here.
- **Round-2 evidence/commit status (2026-07-12):** The previously pending Settings automated and no-JS/unsupported/fatal presentation/recapture evidence is now recorded in the Round-2 acceptance/focused fix records above, with artifacts `artifact://268` and `artifact://269`; Round-2 fix evidence and commit `2dca825` are recorded, while an independent terminal-clean re-review remains pending. Earlier Round-1 state, Help, retry, unit, unaffected UI/manual evidence, hashes, and history remain retained.
- **Round-3 click/range verification status (2026-07-12):** fix evidence and Round-3 review-fix commit `f863c37` recorded; terminal-clean re-review pending — pointer-click Start proof is cited at `tests/browser/lifecycle.spec.ts:62-76` with exploration state/detail/action assertions; keyboard Enter/Space lifecycle coverage remains at `tests/browser/lifecycle.spec.ts:78-119` with the keyboard Retry subflow at `tests/browser/lifecycle.spec.ts:113-117`; current shifted browser anchors are announcement transition `tests/browser/lifecycle.spec.ts:140-153`, Settings `tests/browser/lifecycle.spec.ts:155-179`, drag pause/resume `tests/browser/lifecycle.spec.ts:182-192`, locked pause `tests/browser/lifecycle.spec.ts:194-203`, denied/error/unlocked fallback `tests/browser/lifecycle.spec.ts:205-213`, Retry marker `tests/browser/lifecycle.spec.ts:215-223`, degraded `tests/browser/lifecycle.spec.ts:225-236`, and context-lost/fatal `tests/browser/lifecycle.spec.ts:238-250`; refreshed transition/unit and `src/ui/AppUI.ts` status/focus ranges are recorded above. The terminal result is finding-bearing for TEST-006 and ACC-007; no clean claim is made.
- **Round-3 fix-evidence record (TEST-006, ACC-007; 2026-07-12; prior review history retained):** Parent reran `npm run test:browser -- lifecycle.spec.ts` and it passed 32/32 in 7.4s. The real pointer path in `tests/browser/lifecycle.spec.ts:62-76` calls Playwright `getByTestId('start-button').click()` at line 72, then asserts `data-app-state="exploring"`, the drag-detail copy, and visible Pause at lines 73-75; the separate Enter/Space keyboard loop remains at `tests/browser/lifecycle.spec.ts:78-119`, including keyboard Retry at `tests/browser/lifecycle.spec.ts:113-117`.
- **Round-3 citation-audit evidence (ACC-007; 2026-07-12):** The complete read-only audit at `agent://C02FinalRangeAuditor` found every distinct C02 source/test citation in `PROGRESS.md` current and in bounds, with no stale range: `tests/browser/lifecycle.spec.ts:62-76`, `:78-119`, `:113-117`, `:140-153`, `:155-179`, `:182-192`, `:194-203`, `:205-213`, `:215-223`, `:225-236`, `:238-250`; `src/app/appState.ts:1-387` (including invariants `:83-173`, rejection/result `:176-184`, reducer `:187-386`, retry `:311-385`); `src/app/AppController.ts:1-174`; `src/ui/AppUI.ts:1-980`; `tests/unit/appState.test.ts:1-331` (oracle `:99-125`, retry `:260-274`, illegal/duplicate safety `:276-290`); `src/platform/capabilities.ts:1-62`; `src/app/config.ts:1-23`; `src/main.ts:3-25`; `index.html:18-97`; and `playwright.config.ts:3-25,48-52`. The audit also checked the C02 Round-3 review-table row, the `Latest review round is clean` field, the `Final split review and closure` / `Closure record` roll-up, and the `## C03 — Build the renderer, camera, resize, and resource lifecycle` heading/status; C03 remains `not started` and untouched.
- **Round-3 disposition (prior review record retained):** Fix evidence and commit `f863c37` are recorded; the Result remains `TEST-006, ACC-007`, and terminal-clean re-review is still required; no clean claim is made.
- **Round-4 terminal-clean implementation-review evidence (2026-07-12):** Clean packets `agent://C02TerminalStateReview`, `agent://C02TerminalUIReview`, `agent://C02UltimateTestReview`, and `agent://C02UltimateAccountingReview` reviewed the complete current C02 implementation/fix chain through `f863c37` and the latest evidence ledger `0c8580e`; all prior findings are resolved with no new findings. Result: clean.
- **Planned commit boundary:** `feat: add application lifecycle and capability states`
- **Implementation commit hash(es):** `1a0dcfb` (draft implementation commit), `b4c469d` (review-fix commit), `2dca825` (Round-2 review-fix commit), `f863c37` (Round-3 review-fix commit).
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | STATE-001, UI-001, UI-002, UI-003, TEST-001, TEST-002, TEST-003, TEST-004, ACC-001, ACC-002, ACC-003, ACC-004 | Draft-commit review packets: `agent://C02StateReviewer`, `agent://C02UIReviewer`, `agent://C02TestReviewer`, and `agent://C02AccountingReviewer`; prior evidence is retained. Parent fix verification for all findings: STATE-001 — locked Escape on the forced locked projection transitions to paused with exact `Exploration paused. Resume when you are ready.` copy and visible Resume; UI-001 — final 1280×720 state matrix plus 390×844 onboarding has one live region, no overflow, no settled busy, context-lost says restart required and exposes Retry/Help without busy, fatal exposes Retry/Help with nonblank live status and no busy, and no-JS 320×568 hides overlay/status while leaving the authoritative fallback at `local://c02-evidence/c02-nojs-320x568.png`; UI-002 — Help/Settings make `#app-overlay` and `#app-controls` inert, isolate keyboard to Close before body content, keep background actions unreachable, and return focus on Escape; UI-003 — same-command focus is preserved, including degraded Start Enter/Space and panel-trigger return; TEST-001 — `tests/browser/lifecycle.spec.ts:215-223` drives `/?capability=unsupported-then-supported`; the full-union marker selector `[data-three-runtime], canvas[data-renderer], [data-renderer-runtime], [data-runtime-created]` has count 0 before `Retry` in exact `unsupported` state and count 0 after `Retry` reaches supported onboarding with focused `Start`, and the latest lifecycle rerun passes 32/32 in 7.0s across desktop Chromium 1280×720 and mobile Chromium 390×844; TEST-002 — keyboard-only Help close returns focus to its trigger; TEST-003 — exhaustive `TRANSITION_STATES` × `ALL_EVENTS` oracle in `tests/unit/appState.test.ts:99-125` plus the remaining cases in `tests/unit/appState.test.ts:127-331` proves exact legal/illegal outcomes, identity preservation, and invariant equality; TEST-004 — lifecycle browser rerun passes 32/32 in 7.0s across desktop Chromium 1280×720 and mobile Chromium 390×844 after the corrected locked-paused assertion and announcement mutations are exactly 0 for panel/illegal Escape changes and 1 for the lifecycle transition; ACC-001..004 — parent typecheck `artifact://202`, build `artifact://203`, final production seam inspection, and refreshed durable screenshot set are recorded above. | b4c469d | C02StateReviewer; C02UIReviewer; C02TestReviewer; C02AccountingReviewer / 2026-07-12 | STATE-001, UI-001, UI-002, UI-003, TEST-001, TEST-002, TEST-003, TEST-004, ACC-001, ACC-002, ACC-003, ACC-004 |
| 2 | TEST-005, UI-004, UI-005, ACC-005, ACC-006 | Round-2 read-only re-review packets: `agent://C02StateReReviewer` reports clean with no new state findings; `agent://C02UIReReviewer` finds UI-004 (the no-JavaScript canvas/fallback narrative remains visible behind the authoritative no-JS panel) and UI-005 (unsupported and fatal recovery copy is clipped in the 1280×720 captures); `agent://C02TestReReviewer` finds TEST-005 (Settings automation omits `#app-controls` inertness and Tab/Shift+Tab isolation); `agent://C02AccountingReReviewer` finds ACC-005 (Settings automation overclaim) and ACC-006 (contradictory no-JS/unsupported/fatal captures). Round-2 acceptance/focused fix evidence is now recorded above, including the Settings assertions, no-JS presentation proof, complete unsupported/fatal notices, refreshed captures, and artifacts `artifact://268`/`artifact://269`; Round-2 fix evidence and commit `2dca825` are recorded, and this row remains finding-bearing pending independent terminal-clean re-review. Prior Round-1 evidence and history are retained. | 2dca825 | C02StateReReviewer, C02UIReReviewer, C02TestReReviewer, C02AccountingReReviewer / 2026-07-12 | TEST-005, UI-004, UI-005, ACC-005, ACC-006 |
| 3 | TEST-006, ACC-007 | Terminal review packets: `agent://C02TerminalStateReview`, `agent://C02TerminalUIReview`, `agent://C02TerminalTestReview`, and `agent://C02TerminalAccountingReview`; TEST-006 identifies the pointer-click Start coverage/verification gap, now explicitly represented at `tests/browser/lifecycle.spec.ts:62-76`; click verification is completed and recorded in `f863c37`; ACC-007 identifies stale/shifted C02 tracker source/test ranges, refreshed in the evidence fields above; the exact-range audit is completed and recorded in `f863c37`, with terminal-clean re-review pending. Result remains finding-bearing; no clean claim is made. | f863c37 | C02TerminalStateReview; C02TerminalUIReview; C02TerminalTestReview; C02TerminalAccountingReview / 2026-07-12 | TEST-006, ACC-007 |
| 4 | TEST-006, ACC-007 | Terminal-clean re-review of the current C02 implementation/fix chain through `f863c37` and latest evidence ledger `0c8580e`; the four clean packets `agent://C02TerminalStateReview`, `agent://C02TerminalUIReview`, `agent://C02UltimateTestReview`, and `agent://C02UltimateAccountingReview` resolve the prior findings and report no new findings. | `f863c37` / `0c8580e` | C02TerminalStateReview, C02TerminalUIReview, C02UltimateTestReview, C02UltimateAccountingReview / 2026-07-12 | clean |

- **Latest review round is clean:** yes
- **Blocker:** none
- **Unblock condition:** none
---

## C03 — Build the renderer, camera, resize, and resource lifecycle

- **Status:** `complete`
- **Session/date:** 2026-07-12
- **Dependencies:** C02 complete
- [x] Implement renderer creation, linear color workflow, camera defaults, the visibility-aware animation loop, fixed-step-safe frame timing, and lifecycle disposal.
  - **Implementation evidence (C03-TASK-1-RUNTIME):** `src/app/config.ts:19-32` owns immutable camera defaults (FOV 65°, near 0.08 m, far 550 m, eye height 1.68 m, neutral Z 5 m, +Y world-up, zero roll); `src/render/ThreeRuntime.ts:31-56,80-104,149-215,218-246,301-309,312-324,327-361,375-381` owns renderer/scene/camera setup, consumes the config in the default camera factory and neutral pose, publishes camera/frame/runtime metrics, pauses/resumes the animation loop on document visibility, and disposes initialization, loop, listener, clock, viewport, scene resources, registry, renderer, and finalization; `src/render/frameClock.ts:1-102` establishes a first-sample baseline, clamps each accepted delta to the 0.1-second default, drops paused/hidden time, rejects bad timestamp samples, and provides idempotent terminal disposal; `src/app/AppController.ts:82-120,138-151,194-238` creates/disposes the runtime and installs the development-only lifecycle/rebuild surface.
- [x] Implement CSS-size-driven drawing-buffer resizing, camera projection updates, VisualViewport handling, and explicit DPR/pixel-count caps.
  - **Implementation evidence (C03-TASK-2-VIEWPORT):** `src/platform/viewport.ts:1-178` computes from canvas CSS client dimensions, caps device pixel ratio at 2 and drawing-buffer pixels at 4,100,000, floors the resulting buffer dimensions, updates renderer size/pixel ratio and camera projection, and subscribes/unsubscribes to `ResizeObserver`, window resize, and `VisualViewport` resize/scroll; `src/render/ThreeRuntime.ts:80-104,149-215` constructs the configured camera and initializes/starts the viewport observer as part of transactional runtime initialization.
- [x] Implement shared resource ownership and deterministic scene teardown/rebuild hooks required for quality changes and context restoration.
  - **Implementation evidence (C03-TASK-3-RESOURCES):** `src/render/ResourceRegistry.ts:1-192` implements grouped register/acquire/release ownership, final-owner disposal, rebuild-group disposal, idempotent dispose-all, snapshots/counts, and aggregate disposal errors; `src/render/ThreeRuntime.ts:249-293,327-361` stages and rebuilds neutral scenes, rolls back failed builder/commit paths, disposes prior groups, and runs full runtime disposal; `src/app/AppController.ts:194-238` exposes development-only rebuild and bounded dispose/create-cycle hooks that replace runtimes through the same teardown path.
- **PLAN acceptance contract:** neutral scene renders with correct aspect at every matrix viewport; actual drawing buffer obeys selected pixel cap; hidden/resumed tab has no large delta; repeated create/dispose returns resource counts to baseline; camera begins upright with fixed defaults.
  - **Acceptance evidence (every clause):** Neutral scene/aspect: the corrected focused `npm run test:browser -- viewport.spec.ts` run passed exactly 14/14 in 7.2s across desktop Chromium 1280×720 and mobile Chromium 390×844; helper/matrix setup is covered by `tests/browser/viewport.spec.ts:83-126`, with the matrix cases at `tests/browser/viewport.spec.ts:111-126`, and `tests/browser/viewport.spec.ts:128-144` covers portrait→desktop resize without runtime recreation. Every matrix, resize, and DPR assertion now compares the reported viewport buffer with the actual `HTMLCanvasElement.width/height` across matrix cases `tests/browser/viewport.spec.ts:111-126`, resize `tests/browser/viewport.spec.ts:128-144`, and DPR/cap `tests/browser/viewport.spec.ts:146-164`, while retaining camera-aspect checks; the camera-default assertion is at `tests/browser/viewport.spec.ts:166-176`. The separate lifecycle regression is tracked independently and is not included in the focused 14/14 result. Durable manual neutral capture remains `local://c03-evidence/c03-neutral-1280x720.png`, with canvas CSS/buffer metrics 1246×552 and camera aspect 2.2572. Drawing-buffer cap: the 1920×1080 emulated-DPR-3 measurement was CSS 1886×892, effective DPR 1.5611, actual buffer 2944×1392 = 4,098,048 pixels, within the 4,100,000 cap. Hidden/resumed timing: `tests/browser/viewport.spec.ts:178-260` proves the loop stops while hidden, the frame count stays frozen for 250ms, and the first resumed delta is ≤0.001s and not 0.1s, followed by a positive cadence below 0.1s. Repeated teardown/rebuild: `tests/browser/viewport.spec.ts:262-285` proves ten dispose/create cycles leave one live runtime and stable resource counts; the 1280×720 manual metrics reported resources 2, references 2, groups 1. Upright camera defaults: centralized immutable `APP_CONFIG.camera` (`src/app/config.ts:19-32`) is the source of truth for FOV 65°, near 0.08 m, far 550 m, eye height 1.68 m, neutral Z 5 m, world-up [0,1,0], and roll 0; `ThreeRuntime` consumes it and publishes those values through runtime metrics (`src/render/ThreeRuntime.ts:31-56,80-104,218-246,301-309`). The focused browser assertion at `tests/browser/viewport.spec.ts:166-176` and exact unit/default-factory contract at `tests/unit/threeRuntime.test.ts:156-213` passed. Manual 1280×720 onboarding/running evidence reports camera FOV 65, near 0.08, far 550, position [0,1.68,5], up [0,1,0], roll 0; actual canvas 1246×552 matches the reported metrics, with no horizontal or vertical overflow. Durable camera capture: `local://c03-evidence/c03-camera-1280x720.png`.
- **PLAN focused verification contract:** `npm run test:unit -- frameClock`; `npm run test:browser -- viewport.spec.ts`; manually resize 320×568 → 1920×1080 at DPR 1 and emulated DPR 3; hide/resume tab; run ten runtime create/dispose cycles through a test hook.
  - **Focused-verification evidence (every command/scenario):** Parent `npm run test:unit -- frameClock` passed 1 file/9 tests (9/9; `tests/unit/frameClock.test.ts:9-99`), including the fresh-baseline-after-pause and idempotent-disposal cases. Parent `npm run test:unit -- resourceRegistry` passed 1 file/4 tests (4/4; `tests/unit/resourceRegistry.test.ts:9-104`), including shared final-owner disposal, rebuild-group release, idempotent dispose-all, and cleanup after a throwing disposer. Parent `npm run test:unit -- threeRuntime` passed 1 file/6 tests (6/6; `tests/unit/threeRuntime.test.ts:79-235`), covering late initialization after listener/loop installation with complete unwind, failed rebuild builder rollback, failed rebuild commit rollback, throwing resource disposal that still finalizes renderer/marker/counters, healthy disposal idempotence, actual `installPageHideHandler` persisted/nonpersisted `EventTarget` wiring, and the exact centralized `APP_CONFIG.camera`/default-factory contract. Parent `npm run test:browser -- viewport.spec.ts` selected only the focused viewport suite and passed exactly 14/14 in 7.2s; its helper/matrix coverage is `tests/browser/viewport.spec.ts:83-126` with matrix cases at `tests/browser/viewport.spec.ts:111-126`, resize at `tests/browser/viewport.spec.ts:128-144`, DPR/cap at `tests/browser/viewport.spec.ts:146-164`, camera defaults at `tests/browser/viewport.spec.ts:166-176`, visibility at `tests/browser/viewport.spec.ts:178-260`, and ten-cycle teardown at `tests/browser/viewport.spec.ts:262-285`; the matrix/DPR/resize cases compare actual canvas buffers at every tested state, and the visibility case proves a first resumed delta ≤0.001s, not 0.1s, followed by a positive value below 0.1s. The camera-default browser assertion at `tests/browser/viewport.spec.ts:166-176` passed FOV 65, near 0.08, far 550, position [0,1.68,5], up [0,1,0], and roll 0; the matching unit assertions are at `tests/unit/threeRuntime.test.ts:156-213`. The separate regression `npm run test:browser -- lifecycle.spec.ts` passed exactly 32/32 in 13.9s; it is a separate regression result and is not included in the focused 14/14 count. Manual resize 320×568 → 1920×1080 at DPR 1 and emulated DPR 3, hide/resume, and the development ten-cycle create/dispose hook all passed; the manual 1280×720 onboarding/running camera check recorded FOV 65, near 0.08, far 550, position [0,1.68,5], up [0,1,0], roll 0, actual canvas 1246×552 matching metrics, and no horizontal or vertical overflow. Durable camera capture: `local://c03-evidence/c03-camera-1280x720.png`. Parent `npm run typecheck` passed at `artifact://465`. Parent `npm run build` passed at `artifact://466`; the only noted issue was a nonfatal single-chunk warning above 500 kB. Production marker grep found no matches for `three-runtime:command`, `data-three-runtime-metrics`, or runtime-cycle/rebuild markers.
- **PLAN review surface:** ownership/disposal, resize arithmetic, color-space settings, camera clipping, animation-loop and visibility behavior.
  - **Implementation-review evidence:** Round 1 packets for draft commit `236b001`: `agent://C03RuntimeReviewer` (RUNTIME-001, RUNTIME-002), `agent://C03LifecycleReviewer` (LIFE-001, LIFE-002), `agent://C03TestReviewer` (TEST-001, TEST-002, TEST-003), and `agent://C03AccountingReviewer` (ACC-001, ACC-002, ACC-003). Parent fix verification records the prior implemented fixes and proof for all ten Round-1 findings in the retained acceptance/focused fields and Round-1 record above: BFCache-safe `pagehide` handling, transactional late-init and rebuild rollback, best-effort disposal/finalization, fresh-resume assertions, fail-closed focused browser scope, actual-buffer matrix/resize assertions, exact unit/browser counts, typecheck/build artifacts, and production marker absence. Prior clean read-only packets `agent://C03RuntimeReReviewer`, `agent://C03LifecycleReReviewer`, and `agent://C03TestReReviewer` reviewed the pre-camera fix chain at ledger `07656b0`; their clean results remain historical and do not establish camera-default acceptance or clear Round-2 findings. Round 2 was finding-bearing for RUNTIME-003 and ACC-004; completed post-camera proof (centralized `APP_CONFIG.camera`/runtime metrics, exact unit/browser assertions, and manual capture) plus refreshed source/test anchors are recorded below, and the decisive Round 5 clean re-review below establishes the current clean claim. Prior C03 acceptance/focused evidence and review history remain retained above.
- **Planned commit boundary:** `feat: establish the Three.js rendering lifecycle`
- **Implementation commit hash(es):** `236b001` (draft implementation commit), `fe7e3b6` (review-fix commit), `7c23f02` (Round-2 camera-contract fix commit), `87dd417` (Round-3 tracker-only ACC-004 fix commit), `18e9eba` (Round-4 exact viewport-evidence fix commit). Ledger-only recording commits are excluded from this implementation/review-fix hash list; boundary ledger `0f93498` records `87dd417` and is cited only as review evidence, with no self-reference requirement.
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | RUNTIME-001, RUNTIME-002, LIFE-001, LIFE-002, TEST-001, TEST-002, TEST-003, ACC-001, ACC-002, ACC-003 | Draft-commit Round 1 review packets: `agent://C03RuntimeReviewer`, `agent://C03LifecycleReviewer`, `agent://C03TestReviewer`, and `agent://C03AccountingReviewer`; parent fix verification: RUNTIME-001 — `src/app/AppController.ts:51-58,99-101` forwards the real `PageTransitionEvent` and skips destruction for persisted BFCache pagehide, while `tests/unit/threeRuntime.test.ts:215-235` exercises persisted and nonpersisted events through the actual `installPageHideHandler` EventTarget wiring. RUNTIME-002/LIFE-002 — `src/render/ThreeRuntime.ts:149-215,249-293` unwinds late initialization and transactionally rolls back failed scene builds and rebuild commits; `tests/unit/threeRuntime.test.ts:79-154` verifies listener/loop/viewport/resource/renderer unwind and live-scene/ownership preservation. LIFE-001 — `src/render/ThreeRuntime.ts:327-361` runs best-effort teardown and finalization; `tests/unit/threeRuntime.test.ts:156-213` verifies renderer disposal, loop stop, marker removal, disposed counters, and idempotence even when resource cleanup throws; `tests/unit/resourceRegistry.test.ts:64-104` verifies idempotent and throwing-disposer accounting. TEST-001 — `tests/browser/viewport.spec.ts:178-260` rejects the 0.1 clamp as the first resumed sample and requires a later positive cadence below 0.1s. TEST-002/ACC-001 — the focused `npm run test:browser -- viewport.spec.ts` run is exactly 14/14 in 7.2s, with the separate lifecycle regression exactly 32/32 in 14.1s. TEST-003/ACC-003 — `tests/browser/viewport.spec.ts:83-126` covers helper/matrix setup (matrix cases `tests/browser/viewport.spec.ts:111-126`), `tests/browser/viewport.spec.ts:128-144` covers resize, and `tests/browser/viewport.spec.ts:146-164` covers DPR/cap while comparing actual canvas buffers at every matrix/DPR/resize state. ACC-002 — the acceptance/focused ledger now records the fresh-baseline result, exact command scopes/counts, and parent proof without conflating the historical 46/46 combined total. Typecheck/build and production-marker proof are recorded above; prior evidence/history is retained. Fix evidence and review-fix commit `fe7e3b6` are recorded; clean re-review remains pending and no clean claim is made. | `fe7e3b6` | C03RuntimeReviewer; C03LifecycleReviewer; C03TestReviewer; C03AccountingReviewer / 2026-07-12 | RUNTIME-001, RUNTIME-002, LIFE-001, LIFE-002, TEST-001, TEST-002, TEST-003, ACC-001, ACC-002, ACC-003 |
| 2 | RUNTIME-003, ACC-004 | Camera-contract review found that the pre-fix runtime defaults at `fe7e3b6` drifted from `PLAN.md:41`: FOV 50° vs 65°, near 0.1 m vs 0.08 m, far 100 m vs the calibrated 450–600 m target (the chosen/documented far plane is 550 m within that range), eye height 1.5 m vs 1.68 m, and no explicit zero-roll metric. The mismatch is addressed centrally by immutable `APP_CONFIG.camera` at `src/app/config.ts:19-32`, consumed by the runtime default camera and published in camera metrics at `src/render/ThreeRuntime.ts:31-56,80-104,218-246,301-309`; exact browser/unit assertions passed at `tests/browser/viewport.spec.ts:166-176` and `tests/unit/threeRuntime.test.ts:156-213`. Manual 1280×720 onboarding/running evidence records FOV 65, near 0.08, far 550, position [0,1.68,5], up [0,1,0], roll 0, actual canvas 1246×552 matching metrics, no horizontal or vertical overflow, and durable capture `local://c03-evidence/c03-camera-1280x720.png`. Prior clean packets `agent://C03RuntimeReReviewer`, `agent://C03LifecycleReReviewer`, and `agent://C03TestReReviewer` are retained as clean only for the pre-camera ledger `07656b0` and do not clear this Round 2. ACC-004 is recorded by `agent://C03AccountingReReviewer`; refreshed task evidence uses the stable named anchors `C03-TASK-1-RUNTIME`, `C03-TASK-2-VIEWPORT`, and `C03-TASK-3-RESOURCES` above. | `7c23f02` | C03CameraContractAuditor; C03AccountingReReviewer / 2026-07-12 | RUNTIME-003, ACC-004 |
| 3 | ACC-004 | Independent final review packets: `agent://C03FinalRuntimeReviewer` and `agent://C03FinalAccountingReviewer` found ACC-004 recurring in the tracker-only evidence as stale/out-of-bounds source/test anchors and stale status wording; `agent://C03FinalTestReviewer` and `agent://C03FinalAdversarialReviewer` were clean. Fix evidence: every C03 citation to the shifted `src/platform/viewport.ts`, `src/render/ThreeRuntime.ts`, and `tests/unit/threeRuntime.test.ts` is corrected to exact current anchors (`src/platform/viewport.ts:1-178`; runtime init/rollback `:149-215`, rebuild `:249-293`, scene initialization `:312-324`, disposal/finalization `:327-361`, visibility `:375-381`; runtime tests init/rebuild `:79-154`, disposal/default camera `:156-213`, BFCache `:215-235`, full suite `:79-235`), and the recorded camera-contract status now lists the existing `7c23f02` fix; the tracker-only fix commit `87dd417` is recorded, and only the independent clean re-review remains pending. | `87dd417` | C03FinalRuntimeReviewer; C03FinalAccountingReviewer; C03FinalTestReviewer; C03FinalAdversarialReviewer / 2026-07-12 | ACC-004 |
| 4 | ACC-004 | Terminal packets at boundary ledger `0f93498`: `agent://C03TerminalRuntimeReviewer` found ACC-004 in the shifted `tests/browser/viewport.spec.ts` citations; `agent://C03TerminalTestReviewer`, `agent://C03TerminalAccountingReviewer`, and `agent://C03TerminalAdversarialReviewer` are clean. Exact-anchor fix committed; independent clean re-review pending. | `18e9eba` | C03TerminalRuntimeReviewer; C03TerminalTestReviewer; C03TerminalAccountingReviewer; C03TerminalAdversarialReviewer / 2026-07-12 | ACC-004 |
| 5 | ACC-004 | Decisive clean review packets at boundary ledger `c1af344`: `agent://C03DecisiveRuntimeReviewer`, `agent://C03DecisiveTestReviewer`, `agent://C03DecisiveAccountingReviewer`, and `agent://C03DecisiveAdversarialReviewer`; all prior C03 findings are resolved through the implementation/review-fix chain ending `18e9eba`, with proof retained at `artifact://465`/`artifact://466` plus the exact unit/browser/manual records. No new findings. | `c1af344` | C03DecisiveRuntimeReviewer; C03DecisiveTestReviewer; C03DecisiveAccountingReviewer; C03DecisiveAdversarialReviewer / 2026-07-12 | clean |
- **Round 4 evidence/commit status (2026-07-12):** Terminal runtime packet found ACC-004 in the stale/shifted `tests/browser/viewport.spec.ts` anchors; terminal test, accounting, and adversarial packets are clean. Boundary ledger is `0f93498`; Commit: `18e9eba`; Result: ACC-004; exact-anchor fix committed; independent clean re-review pending. Ledger-only recording commits remain excluded from implementation/review-fix hashes, and no self-reference is required.
- **Round 1 evidence/commit status (2026-07-12):** Parent verification now records implemented fixes and proof for RUNTIME-001/002, LIFE-001/002, TEST-001/002/003, and ACC-001/002/003: exact focused viewport `14/14` in 7.2s, separate lifecycle regression `32/32` in 14.1s, actual canvas-buffer comparisons at every matrix/DPR/resize, first resumed delta ≤0.001s and not 0.1s followed by a positive value below 0.1s, BFCache persisted/nonpersisted wiring, late-init unwind, rebuild builder/commit rollback, throwing-disposal finalization, idempotence, resource-registry accounting, typecheck `artifact://415`, build `artifact://416` with a nonfatal >500 kB warning, and production dist marker absence. Draft `236b001` and review-fix commit `fe7e3b6` are recorded; clean re-review remains pending. The Result remains finding-bearing and the latest review round remains not clean pending independent re-review; prior C03 evidence/history remains retained above.
- **Round 2 evidence/commit status (2026-07-12):** RUNTIME-003 camera-default mismatch and ACC-004 stale task anchors remain finding-bearing; corrected source/test changes are present and parent verification now records the centralized `APP_CONFIG.camera` contract, exact runtime camera metrics, the browser assertion at `tests/browser/viewport.spec.ts:166-176`, the unit/default-factory assertions at `tests/unit/threeRuntime.test.ts:156-213`, and manual 1280×720 onboarding/running values FOV 65, near 0.08, far 550, position [0,1.68,5], up [0,1,0], roll 0 with actual canvas 1246×552, no overflow, and capture `local://c03-evidence/c03-camera-1280x720.png`. The Round-2 camera-contract fix commit `7c23f02` is recorded; only independent clean re-review remains pending. Prior clean packets remain scoped to the pre-camera ledger `07656b0`; the latest review round is not clean.
- **Round 5 evidence/commit status (2026-07-12):** Decisive clean packets `agent://C03DecisiveRuntimeReviewer`, `agent://C03DecisiveTestReviewer`, `agent://C03DecisiveAccountingReviewer`, and `agent://C03DecisiveAdversarialReviewer` reviewed boundary ledger `c1af344`. Commit/Boundary: `c1af344`; Result: clean. The implementation/review-fix chain ends at `18e9eba`; proof remains at `artifact://465`/`artifact://466` and the exact unit/browser/manual records. The ledger-only closure commit is excluded from the implementation/review-fix hash list by declared semantics.
- **C03 closure status (2026-07-12):** All three C03 tasks, PLAN acceptance clauses, focused-verification commands/scenarios, implementation review, required implementation/review-fix commits, and latest clean review are complete. No remaining C03 unchecked, doable, or blocked items. At C03 closure, C04 was the next dependency-ready chunk and was `not started` with all task/evidence fields pending; C04 is now the active chunk, `in progress`, with 3/3 tasks checked, task/acceptance/focused-verification evidence complete, and Round-1 implementation review plus `2ce8704`/`dfc972e` hashes recorded. Round-2 re-review recorded REG-001, DEBUG-004, and ACC-002; their fix evidence and parent verification are now recorded below, while the Round-2 review-fix commit and independent clean re-review/latest-clean predicate remain pending; C04 remains `in progress`.


- **Latest review round is clean:** yes
- **Blocker:** none
- **Unblock condition:** none
---

## C04 — Construct the navigable Badaguan district skeleton

- **Status:** `in progress`
- **Session/date:** 2026-07-12
- **Dependencies:** C03 complete
- [x] Build the south-low/north-high terrain, ten-road 7-by-3 pass-named street layout, sidewalks/paths, garden parcels, walls/gates, coastal edge, a compact public green/park/open-space zone integrated with the street, garden, and coastal system, and protected sight corridors.
  - **Implementation evidence:** `src/world/districtData.ts:24-193` defines the seven east-west and three north-south authored roads, deterministic polyline centerlines, exact names, world/navigable bounds, coherent parcels/setbacks/walls/gates, public-green paths, coast/promenade, collision footprints, route anchors, three sightlines, and explicit authored-inference provenance; `src/world/terrain/createTerrain.ts:21-70` builds the world terrain from the same deterministic ground sampler; `src/world/streets/createStreetNetwork.ts:548-797` renders terrain-conforming road/sidewalk polylines, parcel lawns/setbacks, public-green surfaces/paths/entrances, the coastal promenade and noncollidable sea, and instanced walls/gates/coast screen with its three openings; `src/world/createWorld.ts:11-31` integrates terrain, streets, debug, and navigation. Current contracts are asserted by `tests/unit/districtData.test.ts:94-374` and `tests/browser/district.spec.ts:132-326`. Round-1 fixes retain the 7+3 grid while bounding five of ten curved roads to 4 m, keeping parcel/gate/wall systems outside road-plus-sidewalk corridors, and grounding the 2.6 m selective coast screen. Parent final typecheck passed at `artifact://786`; production build passed at `artifact://787` with only the nonfatal >500 kB warning.
- [x] Define spawn, walkable surface sampling, navigable bounds, soft collision volumes, reset location, and landmark/route anchors in scene data.
  - **Implementation evidence:** `src/world/districtData.ts:69-193` defines the safe `(0,5)` spawn/reset, authored route and landmark anchors, bounds, collision footprints, selective coast, grade, sightline, and provenance data; `src/exploration/navigation.ts:7-131` supplies deterministic ground sampling, radius-aware boundary clamping, swept soft-footprint collision slide/retention, and invalid-input reset; `src/world/createWorld.ts:18-31` publishes the data and navigation functions through the world result. Named tests are `tests/unit/districtData.test.ts:256-373`, `tests/unit/navigation.test.ts:25-138`, and `tests/browser/district.spec.ts:241-326`; the final focused district-data/navigation run passed 2 files/21 tests.
- [x] Add debug-only navigation and composition overlays that can verify road names, bounds, slopes, parcels, the public green/open-space cue, sightlines, and camera height.
  - **Implementation evidence:** `src/world/debug/createWorldDebug.ts:1367-2247` builds the default-hidden DEV-only overlay for road labels/grid, grade, world/navigable bounds, parcels/setbacks, collision footprints, public-green/access paths, coast screened/open sectors, ordered route/anchors, three ribbon-and-arrow sightlines, spawn/reset, and camera height; its filtered `viewLayers` membership is explicit at `:2188-2235`, with static capture layout guards at `:845-938,2073-2078`. `src/render/ThreeRuntime.ts:368-428` DEV-gates visibility, anchor, probe, and allowlisted frame-view commands; `src/app/AppController.ts:194-238` DEV-gates and dispatches the command surface. Named tests are `tests/unit/threeRuntime.test.ts:221-373` and `tests/browser/district.spec.ts:65-122,132-326`; production proof is recorded below. Round-1 fixes isolate the grid view and make sightline/coast evidence diagrammatic rather than label-only.
- **PLAN acceptance contract:** data asserts exactly ten unique roads with 7/3 orientation split; north-to-south grade is visible and testable; required route is continuous and visibly integrates a compact public green/open-space cue; spawn and reset are safe; bounds prevent leaving the authored world; debug view exposes every structural claim without shipping enabled by default.
  - **Acceptance evidence (every clause):** (1) Exact road data is asserted by `tests/unit/districtData.test.ts:94-155` and `tests/browser/district.spec.ts:132-163`; the final focused unit run passed 2 files/21 tests, and final DEV metrics reported exactly 10 unique roads with the 7 transverse/3 longitudinal split, five bounded curved centerlines out of ten, and every via offset at most 4 m. (2) The grade is deterministic and testable in `src/exploration/navigation.ts:7-11`, `tests/unit/navigation.test.ts:25-52`, and `tests/browser/district.spec.ts:214-220`; final DEV metrics were spawn `0`, north `5.31`, south `-0.594`, with the visible grade profile at `src/world/debug/createWorldDebug.ts:1452-1517`. (3) `tests/unit/districtData.test.ts:303-349`, `tests/browser/district.spec.ts:241-276`, and the public-green diagram at `src/world/debug/createWorldDebug.ts:1594-1790` prove the continuous 15-anchor route through the compact public green, uphill stages, coast, and reset; final capture `local://c04-evidence/c04-public-green.png` passed its image gate. (4) Safe spawn/reset `(0,5)` and zero ground height are asserted by `tests/unit/districtData.test.ts:291-301`, `tests/unit/navigation.test.ts:109-119`, and `tests/browser/district.spec.ts:214-224`; final DEV verification confirmed both locations safe. (5) Authored world bounds `[-210,210]×[-300,60]` and navigable bounds `[-200,200]×[-290,38]` are defined at `src/world/districtData.ts:91-93`; `tests/browser/district.spec.ts:278-326` pushed west/east/north/south, checked relocated-footprint collision and noncollidable coast access, and sampled the uphill intersections. (6) Filtered structural debug layers are defined at `src/world/debug/createWorldDebug.ts:2188-2235`; the coast screen is `z=36`, height `2.6 m`, with exactly three road-aligned openings and a noncollidable sea, while `:1452-1517,1792-1954,2010-2078` renders the grade profile, four screened sectors, three open sectors, terrain ribbons, endpoints, leaders, and sightline/coast diagrams. Final captures `local://c04-evidence/c04-grid.png`, `local://c04-evidence/c04-public-green.png`, and `local://c04-evidence/c04-sightlines.png` all passed the final image gates: the road grid/curves, P/A/C/S access diagrams, 01/02/03 plus grade, and readable S1-S4/O1-O3 coast-band identities are clear, with the `02 GREEN VIEW` plate off the corridor. Final production inspection found no reachable `three-runtime:command`/metrics strings and zero reachable command/metrics/global/DOM effect; the compiled bundle still contains an inert hidden `world-debug` stub, so no zero-marker claim is made; the before/after canvas checksum was identical at `a61bbe610bbc4a349d69ca5d2ba8851077b07318ddde1631f90dac4ada7591d0`.
- **PLAN focused verification contract:** `npm run test:unit -- districtData navigation`; browser debug route from spawn through the public green/open-space cue to coast and uphill; boundary push at every edge; ground-height samples across intersections; screenshots of labeled 7×3 grid, public green/open-space integration, and sight corridors.
  - **Focused-verification evidence (every command/scenario):** (1) `npm run test:unit -- districtData navigation` — final parent-observed pass, 2 files/21 tests; `tests/unit/districtData.test.ts:94-374` and `tests/unit/navigation.test.ts:25-138` cover roads/polylines, parcels/gates/walls, relocated future-building footprints, public green/coast, route/sightlines, grade, bounds, collision, reset, and provenance. (2) `npm run test:browser -- district.spec.ts` — final parent-observed pass, exactly 6/6 in 31.1s (`3 logical × 2`); `tests/browser/district.spec.ts:241-276` exercised the full authored route from spawn through public green and uphill stages to coast and reset, with final grid/green/sightline capture paths recorded below. (3) Boundary pushes and ground samples — `tests/browser/district.spec.ts:278-326` exercised west/east/north/south clamping, relocated-footprint collision probes, soft collision slide/retention, safe noncollidable coast access, and samples at all seven authored intersections. (4) Final DEV provenance was one singular run, `npm run dev -- --host 127.0.0.1 --port 5175 --strictPort`, at `http://127.0.0.1:5175/?capability=supported`, desktop Chromium 1280×720 at DPR 1, app state `exploring`, canvas `1246×552`; metrics showed 10 roads (7/3), 5/10 bounded curved roads with max 4 m offset, coherent parcels/gates/walls, world/navigable bounds `[-210,210]×[-300,60]`/`[-200,200]×[-290,38]`, grade `0`/`5.31`/`-0.594`, 15 route anchors, coast screen `z=36`/height `2.6 m`/three aligned openings, noncollidable sea, and isolated `grid`, `public-green`, and `sightlines` views. (5) Final captures `local://c04-evidence/c04-grid.png`, `local://c04-evidence/c04-public-green.png`, and `local://c04-evidence/c04-sightlines.png` all passed their final image gates: road grid/curves, P/A/C/S access diagrams, 01/02/03 plus grade, and readable S1-S4/O1-O3 with the `02 GREEN VIEW` plate off the corridor. (6) Regression/proof counts: `npm run test:unit` — 7 files/53 tests; `npm run test:browser -- viewport.spec.ts` — 14/14 in 20.2s; `npm run test:browser -- lifecycle.spec.ts` — 32/32 in 42.6s; typecheck `artifact://786`; build `artifact://787` succeeded with only the nonfatal >500 kB warning. (7) Final production proof: `npm run build` followed by `npm exec vite preview -- --host 127.0.0.1 --port 41732`; reachable-marker grep found none, the inert compiled `world-debug` stub string was observed, production metrics were null, the DEV global was false, debug DOM count was 0, command dispatches were no-ops, and the before/after canvas checksum remained `a61bbe610bbc4a349d69ca5d2ba8851077b07318ddde1631f90dac4ada7591d0`.
- **PLAN review surface:** scale, route continuity, road count/names, slope, parcels/setbacks, collision simplification, selective coastal exposure. No villa detail review.
  - **Implementation-review evidence:** Round 1 packets for draft commit `2ce8704`: `agent://C04DataAuthenticityReviewer` (DATA-001, DATA-002, COAST-001; COAST-001 normalizes the selective-coast VIS finding), `agent://C04DebugVisualReviewer` (DEBUG-001, DEBUG-002, DEBUG-003; DEBUG-002 normalizes the grid VIS finding and DEBUG-003 its sightline/coast VIS finding), `agent://C04TestAccountingReviewer` (ACC-001; normalized accounting/provenance/production finding), and `agent://C04RuntimeNavigationReviewer` (clean/no new findings). Final fix evidence for every normalized ID is recorded below with current C04 3/3 task/evidence completion, exact final gates, and the Round-1 review-fix hash; road polylines, parcel/gate/wall coherence, 2.6 m/three-opening selective coast, honest inert-stub production wording, isolated grid, diagrammatic sightline/coast proof, and exact final provenance are retained. Independent clean re-review remains pending.
- **Planned commit boundary:** `feat: construct the navigable Badaguan district`
- **Implementation commit hash(es):** `2ce8704` (draft implementation commit), `dfc972e` (Round-1 review-fix commit); Round-2 review-fix commit pending.
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | DATA-001, DATA-002, COAST-001, DEBUG-001, DEBUG-002, DEBUG-003, ACC-001 | Round 1 reviewer packets: `agent://C04DataAuthenticityReviewer`, `agent://C04DebugVisualReviewer`, `agent://C04TestAccountingReviewer`, and clean `agent://C04RuntimeNavigationReviewer`; final fix evidence: DATA-001 — five of ten deterministic road centerlines now use bounded polylines with via offsets ≤4 m while retaining the 7+3 topology; DATA-002 — parcel bounds, setbacks, four-segment walls, and gates are coherent and kept outside every road-plus-sidewalk corridor, with each gate on the nearest declared-road-facing boundary; COAST-001 — terrain-grounded coast screen at `z=36`, height `2.6 m`, exactly three road-aligned openings, noncollidable sea, and four screened/three open debug sectors; DEBUG-001 — production proof says no reachable command/metrics/global/DOM effect and zero disposable debug resources while explicitly acknowledging the inert compiled hidden `world-debug` stub; DEBUG-002 — grid layer contains only road context, bounds, orientation marker, labels/leaders and excludes overview/green/sightline clutter; DEBUG-003 — terrain ribbons, corridor boundaries, arrows/endpoints/leaders, isolated grade, and coast screened/open diagrams are captured in the final sightline proof; ACC-001 — exact post-fix commands, counts, artifacts, URL/viewport/canvas provenance, captures, and checksum are recorded in the final evidence fields. Parent final gates are the 7-file/53-test all-unit pass, 2-file/21-test focused pass, district 6/6 in 31.1s, viewport 14/14 in 20.2s, lifecycle 32/32 in 42.6s, typecheck `artifact://786`, build `artifact://787`, and the final production/manual proof below; review-fix commit `dfc972e` is recorded; independent clean re-review remains pending. | `dfc972e` | C04DataAuthenticityReviewer; C04DebugVisualReviewer; C04TestAccountingReviewer; C04RuntimeNavigationReviewer / 2026-07-12 | DATA-001, DATA-002, COAST-001, DEBUG-001, DEBUG-002, DEBUG-003, ACC-001 |
| 2 | REG-001, DEBUG-004, ACC-002 | Round-2 re-review packets: `agent://C04DataAuthenticityReReviewer` records REG-001; all four future-building soft-collision reservations were relocated into parcel setback interiors, with current source `src/world/districtData.ts:153-157`, navigation `src/exploration/navigation.ts:21-131`, unit proof `tests/unit/districtData.test.ts:256-289` asserting every expanded footprint stays outside every road/sidewalk corridor, and browser probes `tests/browser/district.spec.ts:296-309` proving the formerly blocked named-road point is clear while the intended footprint still collides and resolves outside the reservation. `agent://C04DebugVisualReReviewer` records DEBUG-004; final post-fix `local://c04-evidence/c04-sightlines.png` independently shows `01 UPHILL AXIS`, `02 GREEN VIEW`, and `03 SELECTIVE COAST CORRIDOR >>` with clear terrain ribbons/arrows/endpoints, readable S1-S4/O1-O3 coast-band identities, and the `02 GREEN VIEW` plate off the corridor; static checks in `src/world/debug/createWorldDebug.ts:845-938,2073-2078` plus builder/view-layer ranges `:1367-2247,2188-2235` enforce readable, in-bounds, noncolliding labels and geometry. `agent://C04TestAccountingReReviewer` records ACC-002; the C03 closure cross-reference is corrected at `PROGRESS.md:142,166,168` to state C04 3/3 task/evidence completion, Round-1 review/hash completion, and Round-2 pending commit/latest-clean predicates. `agent://C04RuntimeNavigationReReviewer` reports no new findings. Parent fix verification is recorded below; the Round-2 review-fix commit remains pending and the independent clean re-review remains pending. | pending | C04DataAuthenticityReReviewer; C04DebugVisualReReviewer; C04TestAccountingReReviewer; C04RuntimeNavigationReReviewer / 2026-07-12 | REG-001, DEBUG-004, ACC-002 |

- **Round-1 execution provenance and fix evidence (2026-07-12):** Historical draft-only record `2ce8704` / evidence ledger `6c22b0f` is retained but never final: its earlier command set, DEV run, and pre-fix captures at `local://c04-evidence/c04-grid.png`, `local://c04-evidence/c04-public-green.png`, and `local://c04-evidence/c04-sightlines.png` are superseded by the final post-fix run below; the earlier checksum `f006211037bd866475be09a3d38785cbf82ef607ff0f2bf541a7f41e0bdab8ad` is historical only. Final post-fix provenance is `npm run typecheck` → `artifact://786`; `npm run build` → `artifact://787` (success with only the nonfatal >500 kB warning); `npm run test:unit` → 7 files/53 tests; `npm run test:unit -- districtData navigation` → 2 files/21 tests; `npm run test:browser -- district.spec.ts` → 6/6 in 31.1s; `npm run test:browser -- viewport.spec.ts` → 14/14 in 20.2s; `npm run test:browser -- lifecycle.spec.ts` → 32/32 in 42.6s. The final DEV invocation was singular: `npm run dev -- --host 127.0.0.1 --port 5175 --strictPort`, `http://127.0.0.1:5175/?capability=supported`, desktop Chromium 1280×720/DPR 1, state `exploring`, canvas `1246×552`; final metrics and isolated debug views are recorded above, and only the final post-fix recaptures at `local://c04-evidence/c04-grid.png`, `local://c04-evidence/c04-public-green.png`, and `local://c04-evidence/c04-sightlines.png` passed all image gates, showing road grid/curves, P/A/C/S access diagrams, 01/02/03 plus grade, and readable S1-S4/O1-O3 with the `02 GREEN VIEW` plate off the corridor. Final production was served from `artifact://787` with `npm exec vite preview -- --host 127.0.0.1 --port 41732`; reachable-marker grep found none, the inert compiled `world-debug` stub string was observed, production metrics were null, the DEV global was false, debug DOM count was 0, command dispatches were no-ops, and checksum before/after was identical at `a61bbe610bbc4a349d69ca5d2ba8851077b07318ddde1631f90dac4ada7591d0`.
- **Round-1 fix-evidence/commit status (2026-07-12):** Final fix evidence is recorded for DATA-001, DATA-002, COAST-001, DEBUG-001, DEBUG-002, DEBUG-003, and ACC-001; the review-fix commit `dfc972e` is recorded; independent clean re-review remains pending; the latest Round-1 result remains finding-bearing (`DATA-001, DATA-002, COAST-001, DEBUG-001, DEBUG-002, DEBUG-003, ACC-001`), no clean claim is made.
- **Round-2 evidence/commit status (2026-07-12):** Re-review packets `agent://C04DataAuthenticityReReviewer`, `agent://C04DebugVisualReReviewer`, and `agent://C04TestAccountingReReviewer` record REG-001, DEBUG-004, and ACC-002 respectively; `agent://C04RuntimeNavigationReReviewer` is clean with no new findings. Round-2 fix evidence and parent verification are now recorded above and in the final provenance; the Round-2 review-fix commit is pending. Result remains finding-bearing (`REG-001, DEBUG-004, ACC-002`); the latest review round is not clean and independent clean re-review/latest-clean remains pending. Round-1 evidence, commit `dfc972e`, and prior append-only history remain retained.

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C05 — Create the architectural kit and landmark compositions

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C04 complete
- [ ] Build reusable procedural low-rise villa components for German neoclassical, Gothic/castle-like, and Spanish district silhouettes using stone, brick/tile, stucco, and wood materials; keep Nordic/Danish and Mansard/brick-timber components source-bounded to the Princess Villa and Butterfly Villa compositions unless additional provenance is recorded, and disclose any wider use as artistic inference.
  - **Implementation evidence:** pending
- [ ] Compose varied two- and three-story garden villas with authentic setbacks and restrained signage, avoiding uniform style, oversized massing, and dense row façades.
  - **Implementation evidence:** pending
- [ ] Create source-bounded interpretive compositions for Huashi Building, Princess Villa, and Butterfly Villa, and populate Help/About with a concise user-visible statement that district scale and geometry, parcel and landmark adjacency, exact façades, traditional planting cues, and early-autumn weather are artistic interpretations rather than a survey-accurate or current inventory; keep landmark labels out of the primary view unless requested.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** required route shows at least three unmistakably different district families and all three landmark anchors; Nordic/Danish and Mansard/brick-timber motifs remain landmark-specific unless separately sourced and disclosed; no ordinary villa exceeds three stories; Help/About is keyboard- and touch-reachable and accurately separates sourced facts from artistic inference for compressed geometry/adjacency, façades, traditional-not-current planting, motif reuse, and weather; façades remain readable at Low; repeated meshes share geometry/materials where practical.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** targeted architecture unit/data assertions; capture front/three-quarter silhouettes in neutral debug light and in route context; verify provenance bounds and the complete visible Help/About disclosure by keyboard and touch; walk every parcel edge for clipping/collision; inspect draw calls before/after instancing/merging.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** silhouette variety, modest scale, material restraint, setbacks, landmark-specific provenance bounds, inference-disclosure accuracy/reachability, collision/visible-geometry mismatch, reuse cost.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `feat: add Badaguan villas and landmark silhouettes`
- **Implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C06 — Add road-specific landscape, season, and environmental detail

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C05 complete
- [ ] Implement instanced vegetation and LOD-ready planting for the documented road-specific tree traditions, mixing temperate deciduous canopy with selected evergreens.
  - **Implementation evidence:** pending
- [ ] Establish the early-autumn palette with ginkgo/maple color, leaf-litter accents, deep green canopy, garden understory, and restrained coastal wind motion.
  - **Implementation evidence:** pending
- [ ] Add reusable street, garden, and shore details that preserve sightlines and conservation character without invented commercial clutter.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** all ten roads map to documented primary cues; ginkgo/maple and evergreen corridors are visually distinguishable; canopy frames rather than blocks route/sightlines; Low tier can reduce density without removing road identity; no tropical planting or dominant modern advertising appears.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** planting data assertions; labeled overhead/debug capture; route captures at each representative corridor; reduced-motion and Low-density comparison; transparent sorting and camera-intersection walk-through.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** botanical cue mapping, autumn restraint, instancing/LOD, sightline preservation, motion amplitude, clutter authenticity.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `feat: landscape Badaguan with seasonal planting`
- **Implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C07 — Deliver the coastal lighting and atmosphere

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C06 complete
- [ ] Implement the early-autumn morning sun/sky/environment setup, soft readable shadows, ambient fill, and contact grounding across architecture, foliage, paths, and terrain.
  - **Implementation evidence:** pending
- [ ] Implement restrained marine haze/fog, selective sea and beach glimpses, and a coastal horizon that preserves near-path legibility and protected views.
  - **Implementation evidence:** pending
- [ ] Tune tone mapping, exposure, output color space, materials, shadow bias, and clipping ranges to avoid crushed shade, blown highlights, acne, flicker, z-fighting, and hard fog bands.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** spawn, deepest shade, pale façade, foliage, shore, and horizon remain readable; light direction is coherent; sea is selective; no visible shadow acne/peter-panning/flicker on the review route; all tiers preserve grounding; reduced motion minimizes wind/water motion.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** stable captures at spawn, shade, uphill vista, landmark, and shore in all tiers; luminance/histogram inspection; slow camera pan for shimmer/flicker; near/far and fog-edge walk; reduced-motion comparison.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** time-of-day coherence, exposure/color, fog depth, shadow fit/bias, coast restraint, quality degradation.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `feat: add Badaguan coastal light and atmosphere`
- **Implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C08 — Implement desktop free-roam controls and movement safety

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C07 complete
- [ ] Implement the input action map for WASD physical codes, semantic arrow keys, simultaneous key state, normalized diagonal motion, and delta-time-based walking.
  - **Implementation evidence:** pending
- [ ] Implement pointer-lock mouse look driven by confirmed document events, continuous yaw, clamped pitch, ordinary-lock retry after unsupported raw input, and explicit release/reacquire behavior.
  - **Implementation evidence:** pending
- [ ] Implement the movement/camera state machine, ground following, upright camera, bounds/collision resolution, reset action, held-input clearing, and resume-delta clamping.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** movement distance is frame-rate independent; diagonal speed equals axial speed; pitch never flips and roll stays zero; lock state follows document events; unsupported raw input retries ordinary lock once; denied lock preserves fallback access; camera cannot leave bounds, enter villas, fall through terrain, or run away after lost keyup.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** `npm run test:unit -- input movement navigation`; Playwright synthetic/document-event tests for lock success/error/unlock; manual real pointer-lock route; 30/60/120 Hz distance comparison; blur/hide/orientation/lock-exit held-key tests; boundary/collision circuit.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** browser privilege semantics, action normalization, time integration, camera math, state clearing, collision jitter and reset.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `feat: add safe desktop free-roam controls`
- **Implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C09 — Add input fallbacks, responsive controls, and accessibility

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C08 complete
- [ ] Implement click-drag pointer-capture look plus keyboard movement when pointer lock is unavailable or denied, without repeatedly requesting lock.
  - **Implementation evidence:** pending
- [ ] Implement touch drag-look and an accessible on-screen movement control for coarse-pointer/no-keyboard devices, including cancellation, orientation, safe-area, and scroll suppression behavior; orientation and VisualViewport changes re-layout controls within the visible viewport and safe areas, preserve camera pose and the current mode unless the browser emits lock loss, and clear held input without resetting the route.
  - **Implementation evidence:** pending
- [ ] Complete keyboard-only operation, focus return, visible focus, semantic labels, canvas alternative content, status announcements, zoom/reflow behavior, and reduced-motion handling.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** exploration remains possible after denied/absent pointer lock and denial/error visibly offers drag + keyboard or touch exploration; drag release outside canvas cannot stick; touch supports look plus movement in portrait/landscape; orientation and VisualViewport changes keep controls visible, preserve camera position/rotation and mode except on confirmed lock loss, and clear held input without resetting the route; keyboard-only users can enter, move, open/close UI, reset, and leave; 200% zoom and 320 px width do not hide actions; reduced-motion removes non-essential motion.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** Playwright fallback/accessibility specs; pointer denial with visible fallback copy then drag+keyboard route; keyboard-only full flow; touch emulation at 375×667 and 390×844 in both orientations; during exploration, rotate and resize/scroll VisualViewport and assert control containment, unchanged camera pose/route, preserved mode unless lock loss is emitted, and cleared held input; cancel/multitouch/safe-area scenarios; reduced-motion emulation; automated accessibility scan plus manual focus-order review.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** gesture conflicts, pointer capture, touch ergonomics, visible fallback and focus/state announcements, orientation/VisualViewport pose-mode preservation, reflow/zoom, alternative description, motion comfort.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `feat: add accessible exploration fallbacks`
- **Implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C10 — Make loading, degradation, and GPU recovery resilient

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C09 complete
- [ ] Implement LoadingManager-backed essential/optional asset accounting with honest item progress, indeterminate states, cancellation that exits loading to visible Retry/Return actions and ignores late callbacks, timeout/error classification, and retry.
  - **Implementation evidence:** pending
- [ ] Define and implement procedural-first fallbacks so optional failures preserve the underlying onboarding/exploration/paused mode with a persistent concise degraded notice and recognizable usable scene, while required failures produce an actionable fatal/static alternative.
  - **Implementation evidence:** pending
- [ ] Implement hidden-tab pause/resume and WebGL context-loss/restoration handling that clears input, rebuilds invalid resources, restores scene/camera/settings state, and offers reload/static fallback on recovery failure.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** no failure leaves an indefinite spinner; item progress is accurately labeled; cancellation during indeterminate or known progress reaches a visible non-loading Retry/Return projection and late callbacks cannot revive it; optional 404/malformed/runtime-network failure after shell load preserves the underlying onboarding/exploration/paused mode, camera pose, controls, and persistent degraded notice; cold-start offline navigation is not claimed as supported; required failure offers retry/static content; hidden tabs pause work; successful context restore keeps pose/settings and has valid resources; failed recovery is actionable.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** unit loading classification tests; Playwright route interception after shell load for slow, runtime-network loss, optional 404, malformed, timeout, retry, and cancellation during indeterminate and known progress; assert Retry/Return, stale-callback suppression, and optional failure before and during exploration with mode/pose/control preservation; `WEBGL_lose_context` loss/restore scenario; forced rebuild failure; hidden-tab movement and timing scenario. Do not treat cold-start offline navigation as supported.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** required/optional boundary and shell-loaded offline scope, cancel/abort/retry races, stale callbacks, degraded-mode and notice persistence, state restoration completeness, disposal/rebuild leaks, user-facing errors.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `feat: harden loading and WebGL recovery`
- **Implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C11 — Implement quality tiers and prove performance budgets

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C10 complete
- [ ] Implement persistent Low, Medium, High, and Auto quality selection using one centralized tier contract for render scale, pixel cap, shadows, vegetation, textures, anisotropy, animation, water, and post-processing.
  - **Implementation evidence:** pending
- [ ] Implement capability-aware initial tier selection and conservative hysteresis-based Auto adaptation without changing scene identity or camera state.
  - **Implementation evidence:** pending
- [ ] Add debug metrics and execute reproducible desktop route profiling, shader warm-up, hidden-tab, Auto downshift, resource-leak, and highest-density stress checks against PLAN.md budgets; treat viewport/device emulation as layout/input proof only and physical-mobile thermal profiling as optional, non-gating evidence when an actual device is available.
  - **Implementation evidence:** pending
- **PLAN acceptance contract:** one profile controls all expensive features; tier changes preserve pose and identity; pixel caps are exact; Auto does not oscillate; sustained overload downshifts and stable headroom cautiously upshifts; desktop evidence records the §10 environment including actual graphics renderer/backend and acceleration status, warm-up, duration, sample count, percentile method/results, renderer/resource baselines, and invalid-run conditions; emulation is not labeled physical-mobile performance or thermal proof; optional actual-device evidence records every required device field; ten rebuild cycles do not monotonically leak resources.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** quality selector/unit tests; browser pixel-cap and layout/input assertions across DPR/viewport matrix; run the §10 five-minute desktop protocol at 1280×720 and 1920×1080; use 390×844 emulation only for responsive/input and pixel-cap evidence; synthetic hysteresis clock test; ten tier rebuilds; hidden/resume and stress captures. Use `performance.now()` frame intervals, browser Performance tooling, and renderer metrics, not fps intuition. Run optional physical-mobile thermal profiling only when an actual recorded device is available.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** centralized tier differences, preservation of identity, adaptation stability, desktop measurement reproducibility/validity, bottlenecks, resource counts, accurate emulation limitations, and optional actual-device thermal evidence if supplied.
  - **Implementation-review evidence:** pending
- **Planned commit boundary:** `perf: add adaptive quality tiers and budgets`
- **Implementation commit hash(es):** pending
- **Required desktop performance environment/protocol evidence:** available workstation/browser class: Linux x64, AMD EPYC 9275F (or recorded execution host if different): pending; headed Chromium exact installed version: pending; actual browser-reported graphics renderer/backend: pending; recorded hardware/software acceleration status (do not assume hardware acceleration): pending; AC/no battery throttling: pending; no intentional CPU/network throttling: pending; DPR 1 and selected tier: pending; one complete route warm-up/shader compilation: pending; deterministic five-minute route at 1280×720 and 1920×1080: pending; `requestAnimationFrame` intervals via `performance.now()`: pending; sample count and median/p95/p99: pending; renderer calls/triangles/textures/programs: pending; approximate GPU-memory method/result: pending; browser, viewport, DPR, tier, graphics backend, acceleration, and launch flags: pending; background-load invalidation check and repeat only when invalidated: pending
- **Optional physical-mobile evidence (non-gating; use `not performed` if unavailable):** device model/SoC: pending; OS/browser versions: pending; power/battery state: pending; DPR/tier: pending; warm-up and five-minute route: pending; percentile method/results: pending; thermal/downshift observations: pending; scope label confirms no emulation-based physical-mobile performance or thermal claim: pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **Blocker:** none
- **Unblock condition:** none
---

## C12 — Complete behavioral, visual, and production acceptance

- **Status:** `not started`
- **Session/date:** pending
- **Dependencies:** C11 complete
- [ ] Add deterministic unit and browser coverage for lifecycle transitions, input math/state clearing, pointer-lock denial, drag/touch fallback, resize, reduced motion, loading failures, WebGL2 absence, and context recovery.
  - **Implementation evidence:** pending
- [ ] Add stable visual-review routes and reference captures for spawn, uphill grid, mixed villas, road-specific canopy, landmark silhouettes, coastal sightline, atmosphere, and every quality tier.
  - **Implementation evidence:** pending
- [ ] Execute the full production acceptance matrix, audit provenance/licenses and shipped files, remove debug-only production exposure, and record C12 evidence without deferring any canonical task; land any discovered runtime, configuration, performance, content, or production fix separately with an appropriate Conventional Commit type and rerun affected C12 verification before C12's per-chunk clean review.
  - **Implementation evidence:** pending
- **Executable §12 Production row (C12 and repeatable review evidence):** clean install, typecheck, full tests, green production build, base-path serve and static-host behavior; shipped-file, dependency, provenance, and license audit; no secrets, remote hotlinks, unknown-license assets, unused heavy dependencies, unexpected network calls, or debug-only production exposure; shell-loaded optional runtime-network fallback behavior; cold-start offline navigation remains explicitly unsupported and unclaimed.
- **PLAN acceptance contract:** all required contracts in this plan have passing C12 evidence; visible onboarding/control/lock/fallback copy, cancellation/degraded-mode invariants, orientation/VisualViewport pose-mode preservation, inference disclosure, landmark provenance bounds, and public green/open-space integration are observed in browser/visual review; reference captures are human-reviewed for authenticity and artifacts; production bundle contains no debug UI by default, unknown-license assets, remote hotlinks, secrets, or unused heavy dependencies; clean install/build/serve succeeds. C12 canonical completion requires all 36 canonical `C01`–`C12` tasks checked, every C12 acceptance clause evidenced, every C12 focused-verification command/scenario recorded passing, the C12 per-chunk review's latest round clean, and all C12 implementation/fix commits recorded. The five whole-implementation final-review tasks are later gates and are not prerequisites for C12 completion.
  - **Acceptance evidence (every clause):** pending
- **PLAN focused verification contract:** `npm ci`; `npm run typecheck`; `npm run test:unit`; `npm run test:browser`; `npm run build`; serve `dist` under the configured base path; run visual tests for capture then human-review diffs; execute every runnable §12 matrix scenario, including the executable Production row above; inspect built network requests, shipped files, dependencies, provenance, and licenses; verify any C12 finding has its own appropriate commit and affected C12 checks were rerun before C12's per-chunk clean review. No C12 matrix evidence depends on either later whole-implementation review stream, the post-review rerun, or closure work.
  - **Focused-verification evidence (every command/scenario):** pending
- **PLAN review surface:** entire user journey and plan contract, visible onboarding/control/lock/fallback copy, cancellation/degraded-mode invariants, orientation/VisualViewport pose-mode preservation, inference disclosure and landmark provenance, public green/open-space integration, visual coherence, accessibility, reproducible desktop performance evidence and honest mobile-evidence scope, production artifact/provenance, commit boundaries, regression risk. This is C12's per-chunk review: resolve its findings before marking C12 complete; whole-implementation split reviews follow C12 completion.
  - **Implementation-review evidence:** pending
- **Planned C12 commit boundary:** `test: complete C12 production verification` — C12 acceptance tests, reviewed baselines, provenance records, and C12 evidence only; use separate `fix:`, `feat:`, `perf:`, `build:`, `test:`, or `chore:` commits for discovered changes and rerun affected C12 verification before this commit and C12's clean per-chunk review.
- **C12 implementation commit hash(es):** pending
- **Append-only implementation review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest review round is clean:** no
- **C12 completion gate:** C12 may become `complete` when C11 is complete; all 36 canonical C01–C12 tasks are checked with implementation evidence; C12 acceptance and focused verification are fully evidenced; C12's implementation review is complete with its latest append-only round clean; and every C12 implementation and per-finding fix commit is recorded. The five final-review tasks below are later obligations and MUST NOT be used as C12 completion predicates.
- **Blocker:** none
- **Unblock condition:** none

---

## Final split review and closure

### Post-C12 closure-only gate

This closure-only predicate is outside §12 and is not a matrix row or a prerequisite for completing C12 or either individual whole-implementation review stream. After C12 reaches its canonical completion predicate, closure MUST proceed linearly in this order; the complete §12 matrix remains repeatable during final review/re-verification: run both whole-implementation split reviews; separately commit every finding with an appropriate Conventional Commit type and rerun affected verification; repeat the applicable reviews until both latest append-only rounds are terminal `clean` after all final-review fix commits; rerun the complete required acceptance and verification gates, including every §12 scenario, against that reviewed post-fix state; create the distinct post-review acceptance/evidence-only commit; populate attributable closure; then commit the populated tracker closure. Overall completion is impossible unless all 36 canonical tasks, all five final-review tasks, every chunk acceptance/focused-verification/review gate, both terminal-clean final split reviews, every required implementation and per-finding fix commit, the post-review re-verification and acceptance/evidence commit, and closure attribution are recorded. An unchecked or pending required item always means the overall status is non-complete. Any later finding, fix, or failed gate invalidates affected evidence and requires affected checks plus the complete post-fix rerun before closure.

### Behavioral and engineering review

- **Status:** `not started`
- [ ] Re-run and record the complete automated and manual behavioral acceptance matrix from `PLAN.md` across required browsers, viewports, input modes, failure paths, and quality tiers.
  - **Evidence:** pending
- [ ] Confirm lifecycle, loading, input, movement safety, accessibility, responsive behavior, recovery, quality adaptation, performance, cleanup/disposal, and production packaging have no unresolved findings.
  - **Evidence:** pending
- **Matrix evidence (every §12 row and scenario):** pending
- **Behavioral review completion gate:** this stream may become `complete` when its two tasks and all applicable runnable §12 behavioral/engineering scenarios are evidenced, every finding has a separately recorded typed fix commit, and its latest append-only round is terminal `clean`. It does not require the visual stream to be complete or the later post-review rerun, acceptance/evidence commit, closure attribution, or tracker-closure commit.
- **Append-only final behavioral review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest re-review is terminal clean after all finding-fix commits:** no
- **Per-finding typed fix commit hash(es):** pending
- **Blocker/unblock condition:** none

### Visual authenticity and experience review

- **Status:** `not started`
- [ ] Review stable captures and live routes for the south-low/north-high ten-road district, low-density mixed villas, garden setbacks, the compact public green/open-space integration, road-specific temperate planting, early-autumn morning light, marine haze, selective coastal views, landmark silhouettes and provenance bounds, conservation character, and quality-tier identity.
  - **Evidence:** pending
- [ ] Confirm free movement and view rotation, readable lighting/shadows, atmosphere, landmark and streetscape authenticity, visual stability, reduced-motion behavior, and absence of the authenticity pitfalls enumerated in `PLAN.md`.
  - **Evidence:** pending
- [ ] Confirm Help/About is keyboard- and touch-reachable and clearly distinguishes sourced facts from inferred scale/geometry, adjacency/façades, traditional-not-current planting, landmark-motif reuse, and early-autumn weather; also review compact public green/open-space integration and landmark-specific Nordic/Mansard provenance.
  - **Evidence:** pending
- **Visual/authenticity matrix evidence (every §12 observation):** pending
- **Visual review completion gate:** this stream may become `complete` when its three tasks and all applicable runnable §12 visual/authenticity observations are evidenced, every finding has a separately recorded typed fix commit, and its latest append-only round is terminal `clean`. It does not require the behavioral stream to be complete or the later post-review rerun, acceptance/evidence commit, closure attribution, or tracker-closure commit.
- **Append-only final visual review and fix/re-review rounds:**

| Round | Finding IDs | Review/fix evidence | Commit | Reviewer/date | Result (`clean` or new finding IDs) |
|---|---|---|---|---|---|
| 1 | pending | pending | pending | pending | pending |

- **Latest re-review is terminal clean after all finding-fix commits:** no
- **Per-finding typed fix commit hash(es):** pending
- **Blocker/unblock condition:** none

### Post-review re-verification and acceptance/evidence commit

This stage begins only when both final split reviews are complete and their latest append-only rounds are terminal `clean` after every finding has a separately recorded `fix:`, `feat:`, `perf:`, `build:`, `test:`, or `chore:` commit. Rerun the complete §12 matrix and every required production acceptance/verification gate against that exact post-fix state. Any new failure returns the affected review stream to `in progress`, requires a separate typed fix commit, and invalidates the evidence and commit fields below until both streams are terminal clean again and full re-verification passes.

- **Full post-review §12 matrix and production re-verification evidence:** pending
- **All required gates pass on the terminal-clean post-fix state:** no
- **Post-review acceptance/evidence commit boundary:** `test: record final Badaguan acceptance evidence` — acceptance/evidence changes only; no product, configuration, performance, content, or production fixes.
- **Post-review acceptance/evidence commit hash:** pending
- **Commit is distinct from C12's commit and newer than every final-review finding-fix commit:** no

### Unchecked-item register (non-complete states only)

This register may document only a non-complete `blocked` or `deferred by explicit user approval` state. It never waives, checks, closes, or makes a required task/review/gate complete. `not applicable` is permitted only after a committed `PLAN.md` amendment changes or removes the obligation; cite that commit and the replacement contract. Overall status MUST remain non-complete while this register contains any still-required unchecked item.

| Item ID | Exact unchecked wording | Classification | Evidence / approval or PLAN amendment commit | Owner | Unblock condition or destination | Acceptance impact |
|---|---|---|---|---|---|---|
| none | none | none | none | none | none | none |

### Closure record

- **Overall status:** `in progress`
- **Canonical implementation tasks checked:** 12/36
- **Final-review tasks checked:** 0/5
- **Chunks complete in strict sequence:** 3/12
- **All 12 acceptance contracts fully evidenced:** no
- **All 12 focused-verification contracts fully evidenced:** no
- **All 12 implementation reviews latest-round clean:** no
- **All required implementation and per-finding fix commits recorded:** no
- **Behavioral final review complete and latest re-review terminal clean after all fix commits:** no
- **Visual final review complete and latest re-review terminal clean after all fix commits:** no
- **Full post-review §12 matrix and production re-verification pass:** no
- **Unchecked-item register:** none
- **Post-review acceptance/evidence-only commit hash, distinct from C12 and newer than every final-review fix commit:** pending
- **Closure reviewer/date (non-pending required):** pending
- **Closure decision and evidence (non-pending required; cite clean review rounds, re-verification, and post-review acceptance/evidence commit):** pending
- **Final tracker-closure commit instruction:** after every preceding closure predicate is non-pending/complete, set `Overall status` to `complete`, populate this closure record, and create `chore: close Badaguan implementation tracker`. Record all prior commit hashes in the closure evidence; do not require this commit to contain its own hash, because repository history is the authority for the tracker-closure commit itself.
